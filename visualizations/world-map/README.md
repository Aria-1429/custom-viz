# Custom Viz World Map
![表示例](examples/example.png)

https://github.com/user-attachments/assets/eb299dd9-5c77-4113-a31a-c9b2e8e7cd24

Splunk Dashboard Studio 向けのカスタムビジュアライゼーション（世界地図＋アタックマップ）。

世界地図上に「起点（src）→ 終点（dst）」を弧で描き、弧に沿って光の筋が流れるアニメーションで
流量・イベントの流れを可視化する。**任意の列（ログ種別・ステータス・深刻度など何でもよい）**で
色分けでき、着弾点をホットスポットとして強調表示する。

世界地図データ（`world-atlas`）はビルド時にバンドルされるため、**実行時のインターネット通信は
不要**（本番 Splunk 環境の制約に準拠）。

## 特徴

- データドリブン描画（SPL の結果から起点・終点の弧を自動生成）
- **色分けの基準は任意の列**（ログ種別 / ステータス / プロトコル / 深刻度 …）。深刻度専用ではない
- **色は「カテゴリ名|色」でユーザーが明示指定**（`editor.arrayOfStrings`）。viz が語彙を推測して勝手に色を割り当てることはしない
- 弧に沿って光の筋が流れるアニメーション（速度変更・停止可）
- **編集画面のフィールド選択**（`editor.columnSelector`）で任意の列を緯度経度・色分けカテゴリ等に割り当て可能
- ホットスポットと弧にツールチップ（地点名・カテゴリ・count）
- **凡例クリックで絞り込み**（もう一度押すと解除）
- **ドリルダウン対応**（弧・地点のクリックでトークンを設定し、他パネルと連動）
- **件数に応じた線の太さ**＋**上位 N 件だけ描画**（件数が多いときの視認性・描画負荷対策）
- **近接した地点をまとめる**（画面距離ベースのクラスタリング。ズームすると自然に分離する）
- **凡例に件数を表示**（「表示 420 / 全 600 件」＋カテゴリごとの件数。切り捨てた分が分かる）
- **ホバーで関連する矢印だけを強調**（どこから来てどこへ行くかを追える）
- **地図アプリのような操作**：マウスホイールでズーム、ドラッグで移動、ダブルクリックで初期表示に復帰
- **中心の経度・緯度を指定**して任意の地域を中心に表示（例: 経度 135 で日本中心）
- **ズームすると地名が現れる**（低ズーム＝国名のみ → 拡大につれて主要都市 → 地方都市）
- **地名を日本語で表示できる**（「地名の言語」オプション。国名・都市名とも Natural Earth の
  日本語名データを同梱。サーチ結果由来の `src_name` / `dst_name` は翻訳しない）
- **ズームすると国境が詳細になる**（世界表示は軽量な 110m、ズーム時は 50m 解像度へ自動切替。
  常時 高/低 固定も選べる。ドラッグ・ズーム操作中も詳細度は落ちない）
- **件数（count）のしきい値で色分けするモード**（「色分けモード」で切替。範囲と色は
  編集画面のしきい値エディタで動的に追加・削除できる）
- **カスタムツールチップ**（弧・地点のホバーで即座に表示されカーソルに追従。
  ブラウザ標準の遅いツールチップではない）
- **矢印の始点・終点に地点名を常時表示**（`src_name` / `dst_name`。重なりは自動で間引き）
- タイトル / 凡例 / カテゴリフィルタの表示切替オプション
- ライト / ダークテーマ対応（`useTheme` によるガード付き）
- パネルサイズに自動フィット（コンテナサイズを監視して再描画）
- 空データ・必須フィールド欠損・型不一致に対するガード処理

## データ仕様（フィールド）

| 種別 | フィールド名（候補） | 説明 |
| --- | --- | --- |
| 必須 | `src_lat` / `src_lon` | 起点の緯度・経度 |
| 必須 | `dst_lat` / `dst_lon` | 終点の緯度・経度 |
| 任意 | 色分けカテゴリ（`category` / `type` / `log_type` / `event_type` / `status` / `protocol` / `severity` / `threat_level` / `level`） | **任意の文字列**。深刻度に限らずログ種別・ステータス等でよい。色は編集画面の「色分け」で明示指定する（→ [色分けの設定](#色分けの設定)） |
| 任意 | `count`（`events` / `total`） | イベント数 |
| 任意 | `src_name` / `dst_name` | 起点・終点の表示名（**地図上にラベル表示**＋ツールチップ） |

フィールドの決め方（優先順）:

1. 編集画面「データフィールド」セクションのフィールド選択（任意の列名を割り当て可能）
2. 未選択の場合は上表の候補名で自動判定（大文字小文字を問わない）

## オプション（ダッシュボード編集画面）

| セクション | オプション | 説明 |
| --- | --- | --- |
| データフィールド | 送信元/宛先の緯度・経度 ほか | 使用する列の明示指定（未設定は自動判定） |
| 表示 | タイトルを表示 / 凡例を表示 / カテゴリフィルタを表示 | 各オーバーレイの表示切替 |
| 表示 | タイトルの文字列（`titleText`） | 既定 `GLOBAL THREAT MAP`。**空欄にするとタイトルを出さない** |
| 表示 | アニメーション周期（秒、0で停止） | 光の筋がパスを走り切る秒数。`0` で全アニメーション停止（静的表示） |
| 表示 | 件数による線の太さの強調度（`widthScale`） | `0` で一律の細線。大きいほど count の差が太さに出る（既定 `1`） |
| 表示 | 描画する矢印の上限（`maxArcs`） | count の多い順に N 本だけ描く。`0` で無制限（既定 `0`） |
| 表示 | 地点ホバーで関連する矢印を強調（`highlightOnHover`） | 地点にマウスを乗せると、その地点に繋がる矢印以外を薄くする |
| 表示 | 近接した地点をまとめる半径（`clusterRadius`） | 画面上でこの距離（px）以内の地点を1つにまとめる。既定 `18`、`0` でまとめない。**画面距離なのでズームすると自然に分離する**（→ [地点の集約](#地点の集約)） |
| 表示 | 凡例に「表示◯◯／全◯◯」の件数を表示（`showTotals`） | 既定 ON。`maxArcs` や絞り込みで減っている分を示す（→ [件数の表示](#件数の表示)） |
| 表示 | 凡例のカテゴリごとに件数を表示（`showCategoryCounts`） | 既定 ON。各カテゴリ行の右端に件数を併記する |
| 地図の表示範囲 | 中心の経度 / 中心の緯度 | 地図の中心にする座標（例: 経度 `135` / 緯度 `35` で日本中心）。既定は `0, 0` |
| 地図の表示範囲 | 初期ズーム倍率 | `1` で世界全体。最大 `40`。表示時のズーム状態 |
| 地図の表示範囲 | ホイールズーム・ドラッグ移動を有効にする | OFF にすると地図を固定（静的パネル向け） |
| 地図の表示範囲 | 国境の詳細度（`mapDetail`） | `自動`（既定。ズーム4倍で50m解像度へ・3倍を下回ると110mへ戻るヒステリシス切替）/ `低`（常に110m・軽量）/ `高`（常に50m）。**操作中も詳細度は変わらない**（画面内の国だけを描画して軽量化） |
| 地名ラベル | 地名を表示 | ズームに応じて国名 → 都市名が現れる |
| 地名ラベル | 地名の言語（`labelLang`） | `英語`（既定）/ `日本語`。地図由来の国名・都市名のみ対象（`src_name` / `dst_name` はそのまま） |
| 地名ラベル | 矢印の始点・終点に地点名を表示 | `src_name` / `dst_name` を地図上に常時表示 |
| 地名ラベル | 地名の文字サイズ（px） | 既定 `11`（6〜24） |
| 地名ラベル | 地名の表示量（`labelDensity`） | `0.2`〜`5`（既定 `1`）。大きいほど多く表示（候補数とラベル間隔の両方が連動） |
| 色分け | 色分けモード（`colorMode`） | `カテゴリ列の値`（既定）/ `件数（count）のしきい値`（→ [件数で色分けする](#件数で色分けする)） |
| 色分け | 件数のしきい値と色（`countThresholds`） | モードが「件数」のとき使用。「＋」で範囲を追加でき、各範囲は**下限以上・上限未満** |
| 色分け | カテゴリごとの色（`categoryColors`） | モードが「カテゴリ」のとき使用。1行に1つ「カテゴリ名\|色」。**ここに書いた色だけが使われる**（→ [色分けの設定](#色分けの設定)） |
| 色分け | 上記に無いカテゴリの色（`fallbackColor`） | 未マッピングのカテゴリに使う既定色 |
| 色分け | 凡例・フィルタの並び順（`categoryOrder`） | 1行に1カテゴリ名。書かなかったものは検索結果の登場順で後ろに並ぶ |
| 色分け | 色分けの呼び名（`categoryLabel`） | 凡例の見出し・フィルタの「すべての〇〇」に使う。空欄なら非表示 |
| 背景 / 陸地 | 背景色・陸地色 | チェック ON 時のみカスタム色（transparent 指定で非表示） |

タイトル文字列は既定で `GLOBAL THREAT MAP`。編集画面の「表示」→「タイトルの文字列」で変更できる
（空欄にするとタイトルを表示しない）。

### 地点の集約

同じ都市でも IP ジオロケーションの緯度経度はわずかにばらけるため、そのまま描くと
同一地点に大量のホットスポットが重なって読めなくなる。「表示」→**「近接した地点をまとめる半径」**
（既定 `18`px）で、**画面上の距離**が近い地点を1つのマーカーに統合する。

- **画面距離で判定するため、ズームすると同じ設定値でもクラスタが分離する**
  （拡大するほど同じ px がより狭い地理範囲を指すため）。地図アプリのピンクラスタと同じ挙動。
- 統合された地点は count が合算され、ツールチップに**「ほか N 地点」**と内訳が出る
- 代表座標はクラスタ内の **count 加重平均**（件数の多い地点へ寄る）
- 矢印の始点・終点もクラスタ代表点に吸着するため、終端が数十本に割れて潰れない
- **`0` にすると集約しない**。v1.8.2 以前と同じ見た目に戻したい場合はこれを選ぶ

### 件数の表示

「表示する矢印の上限」（`maxArcs`）は count の多い順に N 本だけを描き、**残りは捨てる**。
捨てられた分の存在が画面に出ないと「全体で何件あるのか」が分からなくなるため、
凡例に件数を出せるようにしている（いずれも既定 ON）。

- **「表示 420 / 全 600 件」** … 絞り込みや `maxArcs` で減っているときの表記。
  全件描いているときは「全 600 件」とだけ出る
- **カテゴリごとの件数** … 各凡例行の右端に桁区切りで併記。
  **絞り込みや上限の影響を受けず、常に全件を数える**（絞り込み中に他カテゴリが
  0 になって「データが無い」と誤読されるのを防ぐため）
- **単位はデータに追従する**。count 列があれば合計イベント数（`件`）、
  無ければ弧の本数（`本`）を数える
- 極小パネル（凡例がコンパクト表示になるサイズ）では地図を覆わないよう自動的に隠れる

### 色分けの設定

**この viz は色を推測しない。** 「カテゴリ名|色」で書いたものだけが色の根拠になる。

色分けの基準は深刻度とは限らない（ログ種別・ステータス・プロトコル・部署など何でもよい）ため、
`critical` が `high` より重い、といった**語彙の意味を viz 側で解釈することは一切しない**。
並び順もユーザー指定（`categoryOrder`）に従い、未指定なら検索結果への登場順になる。

編集画面「色分け」→「カテゴリごとの色」に、1行ずつ次の形式で入力する:

```
high|#ff0000
medium|#e6b93c
low|#38a6ff
```

ログ種別で色分けする場合:

```
auth|#00b0ff
firewall|#ff5a2e
dns|#2dd4bf
```

- 区切りは半角の `|`。前後の空白は無視される
- 大文字小文字は区別しない（`HIGH` と `high` は同じカテゴリ）
- 色は `#RRGGBB` / `#RGB` / `#RRGGBBAA` のほか、`red`・`lime`・`navy` 等の基本的な色名も使える
- **書かなかったカテゴリは「上記に無いカテゴリの色」（既定 `#38a6ff`）**で描かれる。
  凡例ではスウォッチが破線で囲まれ、ホバーすると設定方法が表示される
- 解釈できない行（区切りが無い・色が不正）は**黙って無視**される（描画は壊れない）
- 色分け列の値が空の行は `(未分類)` という独立したカテゴリになる

### 件数で色分けする

「色分け」→「色分けモード」を **「件数（count）のしきい値で色分け」** にすると、
カテゴリ列の代わりに **count の値の範囲**で弧・地点を色分けできる
（通信量マップのようにカテゴリ列が無いデータ向け）。

- 範囲と色は「件数のしきい値と色」で設定する。**「＋」で範囲を動的に追加**できる
- 各範囲は**下限以上・上限未満**。上限を空にすると「以上」の開いた範囲になる
- 凡例・フィルタには範囲名（`0〜100` / `100以上` など）がカテゴリと同じように並び、
  クリックでの絞り込みも同様に効く
- どの範囲にも入らない count は `(未分類)` として「上記に無いカテゴリの色」で描かれる
- このモードでは「カテゴリごとの色」「凡例・フィルタの並び順」は使われない

### 絞り込みとドリルダウン

**凡例クリックで絞り込み**：凡例のカテゴリを押すとその弧だけを表示し、
もう一度押すと解除される（右上のフィルタと連動）。選択中の項目は下線＋太字になる。

**ドリルダウン（インタラクション）**：弧・地点をクリックすると、
ダッシュボードの他パネルへトークンを渡せる。使うには編集画面で設定が要る:

1. パネルを選択 → 右パネルの**「インタラクション」**タブを開く
2. 「トークンを設定」などのアクションを追加する
3. 表示モードで弧または地点をクリックする（**編集モードでは動かない**。下記参照）

viz が渡す値:

| イベント | 発火する要素 | 主な payload |
| --- | --- | --- |
| `link.click` | 矢印（弧） | `row.src_name.value` / `row.dst_name.value` / `row.category.value` / `row.count.value` / 緯度経度4種 |
| `point.click` | 起点・終点の地点 | `row.name.value` / `row.category.value` / `row.count.value` / `row.role.value`（`source` or `target`） |

> **注**: トークンが実際に入るかはダッシュボード側のインタラクション定義に依存する
> （viz は「クリックされた事実」を発火するだけで、何をするかはホストが決める）。
> ローカル検証では「要素ごとに正しい payload が登録されているか」までを確認している。

### 地図の操作方法

| 操作 | 動作 |
| --- | --- |
| マウスホイール | カーソル位置を中心にズーム（`1`〜`40` 倍） |
| ドラッグ | 地図を移動（パン） |
| ダブルクリック | 初期表示（オプションで指定した中心・ズーム）に戻す |
| 右下の `×N ⟲` | 同上（ズーム中のみ表示） |

> **注意**: ダッシュボードの**編集モードでは操作できません**。Splunk が編集中にカスタム viz の
> iframe への入力を遮断する仕様のためで、**表示モードでは正常に動作します**。編集中に見え方を
> 決めたい場合は「中心の経度・緯度」「初期ズーム倍率」を右パネルから設定してください。

## 地図データの出典・ライセンス

同梱データはいずれも**パブリックドメイン**で、クレジット表記は不要です。

| データ | 出典 | ライセンス |
| --- | --- | --- |
| 国境ポリゴン・国名（110m / 50m） | [world-atlas](https://github.com/topojson/world-atlas)（Natural Earth の再配布） | パブリックドメイン |
| 都市名（約 7,340 件・日本語名含む） | [Natural Earth](https://www.naturalearthdata.com/about/terms-of-use/) `ne_10m_populated_places`（`NAME` / `NAME_JA`） | パブリックドメイン |
| 国名の日本語対応表 | Natural Earth `ne_110m/50m_admin_0_countries` の `NAME_JA` から抽出 | パブリックドメイン |

> Natural Earth 公式の利用条件：*"All versions of Natural Earth raster + vector map data found on
> this website are in the public domain."* / *"No permission is needed to use Natural Earth.
> Crediting the authors is unnecessary."*

なお、リポジトリ同梱の `major_cities.csv`（SimpleMaps 由来）は **Splunk 側の lookup 用**であり、
出典表記が必要なライセンスのため **viz のバンドルには含めていません**。

## 開発

```bash
yarn install
yarn build          # visualizations/*/dist を生成
yarn verify         # happy-dom によるローカル検証（Splunk 実機なしでバンドルを検証）
yarn package        # dist/*.spl（Splunk アプリパッケージ）を生成
```

本番向け（minify・ソースマップ無し）は `yarn build:prod` の後に `yarn package` を実行する。

アプリのメタデータ（version / label / author / description / category）は `package/app/app.conf`
に格納されている。Splunk 上での見え方を変えるときはこのファイルを編集する（`package.json` は
Node/npm 用）。

## デプロイ（再インストール・再起動なし）

1. `npm version patch --no-git-tag-version && yarn build:prod && yarn package` でバージョンを上げて `.spl` を生成
   （**`yarn build` は開発ビルド**。ソースマップ入りの巨大な `.spl` になるので配布には使わない）
2. Splunk Web「Install app from file」で **"Upgrade app"（上書き）にチェック**して `.spl` をアップロード
3. ブラウザで `https://<host>:8000/en-US/_bump` を開き **Bump version**（Splunk 再起動の代替）
4. ブラウザをハードリロード（Ctrl+Shift+R）

## サンプル SPL

lookup 不要（座標を直接指定。フィールド名は自動判定される）:

```spl
| makeresults format=csv data="src_lat,src_lon,dst_lat,dst_lon,severity,count,src_name,dst_name
51.51,-0.13,35.68,139.69,low,120,London,Tokyo
31.23,121.47,35.68,139.69,high,300,Shanghai,Tokyo
55.76,37.62,35.68,139.69,high,50,Moscow,Tokyo
-23.55,-46.63,35.68,139.69,medium,80,Sao Paulo,Tokyo
28.61,77.21,35.68,139.69,medium,60,Delhi,Tokyo
40.71,-74.01,35.68,139.69,low,10,New York,Tokyo
-33.87,151.21,35.68,139.69,scan,40,Sydney,Tokyo"
```

`major_cities` lookup を使う場合:

```spl
| makeresults format=csv data="src_city,dest_city,severity,count
London,Tokyo,low,1000
Shanghai,Tokyo,high,2000
Moscow,Tokyo,high,3000
Sao Paulo,Tokyo,medium,4000
Delhi,Tokyo,medium,5000
New York,Tokyo,low,6000
Los Angeles,Tokyo,low,7000
Dallas,Tokyo,medium,8000
Sydney,Tokyo,low,9000"
| lookup major_cities city as src_city OUTPUT lat as src_lat lon as src_lon
| lookup major_cities city as dest_city OUTPUT lat as dest_lat lon as dest_lon
```

深刻度以外（ログ種別）で色分けする場合。編集画面の「色分け」に
`auth|#00b0ff` / `firewall|#ff5a2e` / `dns|#2dd4bf` を入力し、
「色分けの呼び名」に `ログ種別` と入れると凡例の見出しになる:

```spl
| makeresults format=csv data="src_lat,src_lon,dst_lat,dst_lon,log_type,count,src_name,dst_name
51.51,-0.13,35.68,139.69,auth,120,London,Tokyo
31.23,121.47,35.68,139.69,firewall,300,Shanghai,Tokyo
55.76,37.62,35.68,139.69,dns,50,Moscow,Tokyo
-23.55,-46.63,35.68,139.69,auth,80,Sao Paulo,Tokyo
28.61,77.21,35.68,139.69,proxy,60,Delhi,Tokyo
40.71,-74.01,35.68,139.69,firewall,10,New York,Tokyo"
```

件数のしきい値で色分けする場合（カテゴリ列なし。編集画面の「色分け」→「色分けモード」を
「件数（count）のしきい値で色分け」にし、しきい値エディタで範囲と色を設定する）:

```spl
| makeresults format=csv data="src_lat,src_lon,dst_lat,dst_lon,count,src_name,dst_name
51.51,-0.13,35.68,139.69,120,London,Tokyo
31.23,121.47,35.68,139.69,300,Shanghai,Tokyo
55.76,37.62,35.68,139.69,50,Moscow,Tokyo
-23.55,-46.63,35.68,139.69,80,Sao Paulo,Tokyo
28.61,77.21,35.68,139.69,60,Delhi,Tokyo
40.71,-74.01,35.68,139.69,10,New York,Tokyo"
```

ズーム・地名表示の確認用（国内の近距離データ。**初期ズーム倍率 `6` / 中心の経度 `137` /
中心の緯度 `36`** を設定すると、都市名ラベルが出る様子を確認できる。
「地名の言語」を `日本語` にすると地名が日本語になる）:

```spl
| makeresults format=csv data="src_lat,src_lon,dst_lat,dst_lon,severity,count,src_name,dst_name
34.69,135.50,35.68,139.69,high,320,Osaka,Tokyo
35.18,136.91,35.68,139.69,medium,140,Nagoya,Tokyo
43.06,141.35,35.68,139.69,low,60,Sapporo,Tokyo
33.59,130.40,35.68,139.69,high,210,Fukuoka,Tokyo
38.27,140.87,35.68,139.69,medium,90,Sendai,Tokyo
34.39,132.46,35.68,139.69,low,45,Hiroshima,Tokyo"
```

---

## リリースノート

このセクションは本ビジュアライゼーションのバージョン履歴を記録します。
新しいバージョンをパッケージ化するたびに、履歴の先頭（下の区切り線の直下）に新しいエントリを追記してください。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) に準拠し、バージョンは [セマンティックバージョニング](https://semver.org/lang/ja/) に従います。
変更種別: `追加` / `変更` / `修正` / `削除` / `非推奨` / `セキュリティ`。

---

### [1.9.0] - 2026-08-02

#### 追加

- **近接した地点をまとめる（クラスタリング）**（「表示」→「近接した地点をまとめる半径」、
  `editor.slider` 0〜80px・既定 `18`）。画面上の距離で集約するため、**ズームすると
  クラスタが自然に分離する**（地図アプリのピンクラスタと同じ挙動）。
  集約された地点のツールチップには「ほか N 地点」と内訳が出て、count は合算される。
  - `0` にすると集約しない（v1.8.2 と同じ 0.1px 完全一致の挙動に退避）。
- **凡例の件数サマリー**（「表示」→「凡例に「表示◯◯／全◯◯」の件数を表示」、既定 ON）。
  `maxArcs` や凡例フィルタで描画対象が減っているときに
  **「表示 420 / 全 600 件」**と出し、切り捨てられた分の存在が分かるようにした。
  全件描いているときは「全 600 件」とだけ出る。
  - 単位はデータに追従する。**count 列があれば合計イベント数（件）、無ければ弧の本数（本）**。
- **凡例のカテゴリごとの件数表示**（「表示」→「凡例のカテゴリごとに件数を表示」、既定 ON）。
  各カテゴリ行の右端に件数を桁区切りで併記する。
  - **カテゴリ別の件数は絞り込みや `maxArcs` の影響を受けない**（常に全件を数える）。
    描画中の分で数えると、凡例クリックで絞り込んだときに他カテゴリが 0 になり
    「そのカテゴリのデータが無い」と誤読させるため。切り捨てられた分は
    上の「表示◯◯／全◯◯」で示す。
  - いずれの件数表示も、極小パネル（凡例がコンパクト表示になるサイズ）では
    地図を覆わないよう自動的に非表示になる。

#### 修正

- **近接した地点がほとんど集約されていなかった**問題を修正した。
  v1.8.2 までの集約は「投影後の座標を小数1桁で丸めたキーの完全一致」だったため、
  実質 0.1px 以内に重なった点しかまとまらなかった。IP ジオロケーションのデータは
  同一都市でも緯度経度がわずかにばらけるため、同じ都市に数百個のホットスポットが
  重なって描かれていた。上記のクラスタリングで解消している。
- **負の `count` でクラスタの代表点が地図外へ飛ぶ**問題を修正した（自己レビューで発見）。
  `| eval count=a-b` のような差分を count 列に入れると加重平均の重みが負になり、
  マーカーがクラスタの構成点から大きく外れた位置に描かれていた
  （実測: x=100 と x=110 の2点に count=10 / -9 を与えると代表点が x=10）。
  重みを 0 以上に丸め、合計が 0 以下のときは単純平均へ退避するようにした。

#### 変更に伴う注意

- **既定で集約が有効（18px）になるため、既存ダッシュボードの見た目が変わる**
  （近接した地点が1つのマーカーに統合される）。従来の見た目に戻すには
  「近接した地点をまとめる半径」を `0` にする。

#### 成果物

- `dist/custom_viz_worldmap-1.9.0-a7a9bf8.spl`
- ローカル検証（`yarn verify`）: **160 件すべて成功**（今回 17 件追加。
  クラスタリングの集約・ズームでの分離・count 合算・負の count での代表点保護と、
  件数サマリーの単位切替・切り捨て表示・絞り込み時の据え置き・
  オプションでの非表示を検証）。

---

### [1.8.2] - 2026-07-30

#### 追加

- **`THIRD_PARTY_NOTICES.txt` を `.spl` に同梱**するようにした。バンドルしている
  OSS のライセンス条文と同梱素材の出典を、esbuild metafile（実際にバンドルされた
  モジュールの一覧）から機械生成している。パッケージ時に内容の鮮度も検査され、
  依存が変わったまま再生成し忘れると `.spl` 生成が失敗する。

#### 成果物

- `dist/custom_viz_worldmap-1.8.2-b4e4674.spl`

---


### [1.8.1] - 2026-07-30

#### 変更

- **ドラッグ・ズーム操作中に国境の詳細度が落ちなくなった**。
  v1.8.0 の「操作中は 110m で描き、静止後に 50m へ差し替える」段階的描画は、
  操作のたびに地図が粗くなり操作感が悪いという指摘を受けて廃止した。
  - 50m を操作中も使い続けるための対策として、**画面に映っている国だけを
    パス生成する**ビューポート絞り込みを導入した（経緯度バウンディングボックスの
    事前計算＋画面枠の逆投影による表示範囲判定）。
    実測：全 242 国 約60ms/フレーム → 日本付近 zoom6 で 15 国 約13ms/フレーム。
  - `自動` の切替ズームは **上げ=4倍 / 下げ=3倍のヒステリシス**にした
    （実測で zoom3 の 50m は絞り込み後も約31ms/フレームと重いため上げを4倍にし、
    しきい値付近のズーム往復で詳細度がパカパカ切り替わらないよう下げを3倍にずらした）。
  - `高` 固定を広域表示（zoom<2）で使う場合のみ、従来どおり全件描画になるため
    パンがやや重くなる（約60ms/フレーム）。気になる場合は `自動` を推奨。

#### 成果物

- `dist/custom_viz_worldmap-1.8.1-a7229ca.spl`
- ローカル検証（`yarn verify`）: **143 件すべて成功**（今回 3 件追加。
  「カメラ静止を待たずに詳細国境が出る」回帰テストと、ヒステリシスの往復動作を検証）。

---

### [1.8.0] - 2026-07-30

#### 追加

- **地名の日本語表示**（「地名ラベル」→「地名の言語」、`editor.select`）。
  国名・都市名とも Natural Earth の日本語名（`NAME_JA`・パブリックドメイン）を同梱し、
  地図由来のラベルを日本語に切り替えられる。サーチ結果由来の
  `src_name` / `dst_name` は翻訳しない（データが主役のため）。
  国名は正式名称（例: `中華人民共和国`）で収録されている。
- **件数（count）のしきい値で色分けするモード**（「色分け」→「色分けモード」）。
  範囲と色は `editor.threshold`（「＋」で動的に追加・上限なしの開いた範囲も可）で設定する。
  範囲は**下限以上・上限未満**。しきい値バンドは内部的に擬似カテゴリとして扱われるため、
  凡例・フィルタ・凡例クリックでの絞り込み・ドリルダウンがカテゴリ色分けと同様に機能する。
  どの範囲にも入らない count は `(未分類)` → 既定色。
- **国境の詳細度**（「地図の表示範囲」→「国境の詳細度」）。
  `自動`（既定）はズーム3倍以上で Natural Earth 110m → 50m 解像度へ切り替える。
  常時 `高` / `低` の固定も選べる。50m はパス生成が重いため、
  **ドラッグ・ズーム操作中は 110m で描き、操作が止まった時点で 50m へ差し替える**
  （地図アプリと同じ段階的描画。カクつきを避けるための意図的な挙動）。
- **カスタムツールチップ**。従来のブラウザ標準ツールチップ（SVG `<title>`）は
  表示までが遅くスタイルも当たらなかったため、パネル調の自前ツールチップに置き換えた。
  ホバーで即座に表示され、カーソルに追従し、パネル端では自動で反転する。
  カーソル追従は DOM 直接更新で行い、mousemove のたびに再レンダリングしない。
  当たり判定要素には同内容の `aria-label` を付与している。

#### 変更

- **パン / ズーム操作中の地名ラベル再計算を間引いた**。
  ラベル選定（全都市約 7,300 件の走査＋重なり判定）は毎フレームやるには重いため、
  「カメラが 150ms 静止した時点」でのみ実行し、操作中は選定済みラベルを
  現在のカメラで投影し直すだけにした（低スペック環境のカクつき対策）。
  操作中は一瞬古い選定のまま流れ、止まると確定する。
- カテゴリフィルタ・描画上限（`maxArcs`）の適用を投影の前段へ移動した
  （動作は同じ。ラベル選定の依存をカメラ操作から切り離すための内部整理）。

#### 修正

- **`.spl` に開発ビルドが同梱されていた問題を修正**。v1.7.0 までの `.spl` は
  非 minify の JS＋6.3MB のソースマップ入り（計 9.5MB 相当・圧縮後 1.9MB）だった。
  本版から本番ビルド（minify・ソースマップ無し）で梱包しており、
  50m 国境と日本語地名データを追加したにもかかわらず `.spl` は **729KB** に縮んだ。

#### 成果物

- `dist/custom_viz_worldmap-1.8.0-a7229ca.spl`
- ローカル検証（`yarn verify`）: **140 件すべて成功**（今回 25 件追加。
  日本語地名・count しきい値色分け・国境詳細度の切替・カスタムツールチップ・
  ラベル選定の静止後確定を検証。本番ビルドのバンドルでも全件成功を確認）。

---

### [1.7.0] - 2026-07-28

#### 追加

- **タイトル文字列を編集画面から変更できるようにした**（「表示」→「タイトルの文字列」）。
  従来は `optionsSchema` にキーはあるものの editor 項目が無く、
  ダッシュボード定義のソース編集でしか変更できなかった。
  - **空欄にするとタイトルを表示しない**。空文字を「タイトル無し」の意思表示として
    尊重し、既定値へ戻さない（キー自体が無い＝未設定のときだけ既定値）。

#### 変更

- 編集画面のラベル「深刻度フィルタを表示」を **「カテゴリフィルタを表示」** に修正。
  v1.4.0 で色分けを深刻度専用から任意のカテゴリへ一般化した際の表記漏れ。

#### 見送り（実装したが取り消した）

- 日付変更線（経度180度）をまたぐ矢印を**画面の端で2区間に分割**する変更を一度入れたが、
  **実機で弧が途中で途切れて見えたため取り消した**。
  幾何計算としては最短経路を選べていたものの、
  Natural Earth 図法では東京→ニューヨークのように
  **「地図上では直線で繋がって見える経路」まで分割対象になり**、
  太平洋の真ん中で線が切れて見えてしまう。
  矢印は**始点から終点まで1本の連続した曲線**で描く従来の挙動に戻した。

#### 成果物

- `dist/custom_viz_worldmap-1.7.0-ea92401.spl`
- ローカル検証（`yarn verify`）: **115 件すべて成功**（今回 4 件追加）。
  日付変更線の分割を取り消したため、そのぶんの検証（6件）も併せて削除している。

---

### [1.6.0] - 2026-07-28

#### 修正

- **ズームするほど地名が減っていた不具合を修正**（最大の実害）。
  `CITY_LABELS.slice(0, N)` で**先に「世界の重要度上位N件」を取ってから**
  画面内かどうかを判定していたため、拡大すると上位N件がほぼ全て画面外になり、
  結果として表示できる地名がごくわずかになっていた。
  - 実測（修正前・日本中心）: `zoom6=9件` → `zoom16=7件` → `zoom40=3件`
    （`zoom40` は上限600件を許可していたのに実際は3件しか出ていなかった）
  - **先に画面内へ絞り、そのうえで重要度順に採る**方式へ変更。
  - 実測（修正後・同条件）: `zoom6=80件` / `zoom16=53件` / `zoom40=34件`
- **「地名の表示量」を上げても表示数がほとんど変わらなかった点を修正**。
  件数の上限だけ動かしてもラベルの重なり判定が固定のままだったため、
  実際の表示数が頭打ちになっていた。**候補数とラベル間隔の両方**を
  `labelDensity` に連動させ、増減の両方向に効くようにした
  （zoom16・日本中心での実測: `density0.2=25件` … `density1=53件` … `density3=91件`）。

#### 変更

- 同梱する都市データを **Natural Earth `ne_50m_populated_places`（約1,250都市）から
  `ne_10m_populated_places`（約7,340都市）へ差し替え**。
  50m 版は日本付近に11市しか無く、市町村レベルまで拡大すると
  地名が尽きていたため（データ量の限界であり不具合ではない）。
  10m 版では同じ範囲に44市あり、岡山・高松・川崎・宇都宮などの地方都市も表示される。
  **ライセンスは変わらずパブリックドメイン（クレジット表記不要）**。
  - バンドルサイズ: 都市データ 38KB → 221KB（`.spl` は約 1.73MB → 1.91MB）

#### 追加

- **地名の表示量オプション**（`labelDensity`、`editor.slider` / `0.2`〜`5`）。
  地図が地名で埋まりすぎる／少なすぎる場合に調整できる。

#### 成果物

- `dist/custom_viz_worldmap-1.6.0-ea92401.spl`
- ローカル検証（`yarn verify`）: **111 件すべて成功**（今回 8 件追加）。
  地名が減る不具合は**修正前のコードで再現して失敗することを確認**したうえで
  回帰テスト化している（`zoom16=8件 / zoom40=7件` で失敗 → 修正後は 53件 / 34件）。

---

### [1.5.0] - 2026-07-28

#### 修正

- **右上のカテゴリフィルタを押してもドロップダウンが開かない不具合を修正**
  （ズームを無効にしたときだけ開く、という症状）。
  原因は、パン用に**コンテナの `pointerdown` で即座に `setPointerCapture` していた**こと。
  ポインタが捕捉されると以降のイベントがすべてコンテナへ回され、
  `Select` が自身の `pointerup` / `click` を受け取れなくなっていた。
  - オーバーレイUI（フィルタ・凡例・ズームリセット）に `data-viz-ui="1"` を付け、
    **その内側から始まった操作ではパンを開始しない**ようにした。
  - さらに**ポインタ捕捉を「実際に動き始めてから」に遅らせた**。
    クリック（押して離すだけ）では捕捉が起きないため、
    オーバーレイUIやドリルダウンのクリックを妨げない。
  - オーバーレイUI上のホイールは地図のズームにしない（リストのスクロールを妨げないため）。

#### 追加

- **凡例クリックで絞り込み**。カテゴリを押すとその矢印だけを表示し、
  もう一度押すと解除する（右上のフィルタと連動）。選択中は下線＋太字、対象外は薄く表示。
- **ドリルダウン（インタラクション）対応**。`config.json` に
  `showDrilldown` / `hasEventHandlers` / `canSetTokens` と
  `events`（`link.click` / `point.click`）を宣言し、
  弧と地点を `addDrilldownListener` で**要素ごとに**登録した。
  クリックでダッシュボード全体を絞り込める（トークン設定は編集画面の
  「インタラクション」で定義する）。
  - `payloadCallback` は**要素ごとに固有の値**を閉じ込めている
    （使い回すと「どこを押しても1行目の値が飛ぶ」不具合になるため）。
- **件数による線の太さの強調度**（`widthScale`）。
  従来の固定式 `min(1+√count×0.12, 2.2)` は**count が 10 でも 10000 でも
  太さがほぼ変わらなかった**ため、表示中データの最小〜最大を基準に
  相対的な太さを決める方式へ変更。`0` で一律の細線。
- **描画する矢印の上限**（`maxArcs`）。count の多い順に N 本だけ描く。
  件数が多いサーチでの視認性と描画負荷の対策（`0` で無制限）。
- **ホバーで関連する矢印を強調**（`highlightOnHover`）。
  地点や弧にマウスを乗せると、その地点に繋がる矢印以外を薄くする。
  SVG のベース軌道と Canvas の光の帯の**両方**に適用される。
- 弧に透明な太い当たり判定を重ね、細い線でもホバー／クリックしやすくした。

#### 成果物

- `dist/custom_viz_worldmap-1.5.0-ea92401.spl`
- ローカル検証（`yarn verify`）: **103 件すべて成功**（今回 18 件追加）。
  ドロップダウンの不具合は**修正前のコードで再現して失敗することを確認**したうえで
  回帰テスト化している（「フィルタからのドラッグでは地図が動かない」かつ
  「地図本体からのドラッグでは動く」）。

---

### [1.4.0] - 2026-07-28

#### 変更（破壊的）

- **色分けを「深刻度（severity）専用」から「任意のカテゴリ列」へ一般化した。**
  この viz の色の根拠は深刻度とは限らず、ログ種別・ステータス・プロトコル・部署など
  何でもありうる。従来は `KNOWN_SEVERITY_ORDER`（`critical`→`high`→`medium`→`low`）で
  **viz が勝手にカテゴリを並べ替え、その順にパレットを配っていた**ため、
  深刻度以外の語彙（`auth` / `firewall` / `dns` 等）では
  **並び順に意味が無いのに色だけ自動で決まってしまう**という問題があった。
- **色は完全にユーザー指定のみに依存する方式へ変更**（`editor.arrayOfStrings`）。
  「カテゴリ名|色」を1行ずつ書き、**書いたものだけが色の根拠になる**。
  viz 側の語彙推測・自動割り当ては一切行わない。
  未指定のカテゴリは `fallbackColor`（既定色）で描き、凡例では破線枠＋ホバー説明で
  「色が未設定である」ことを示す。
- 並び順も `categoryOrder`（`editor.arrayOfStrings`）でユーザーが指定する。
  未指定分は検索結果への登場順（＝同じサーチなら毎回同じ＝決定的）。
- ホットスポットの代表色（同一地点に複数カテゴリが集まる場合）も
  「表示順が先のカテゴリ」＝**ユーザー指定順**に従うようにした。
  「深刻度が高い方を優先」のような意味的判断は行わない。
- オプション名を用途に合わせて改名：`severityField` → `categoryField`、
  `severityColors`（パレット）→ `categoryColors`（明示マッピング）。
- 必須フィールド欠損時のメッセージを日本語化した。

#### 削除

- `severityColors`（`editor.seriesColors` の順序付きパレット）と、
  語彙による自動並べ替え（`KNOWN_SEVERITY_ORDER`）。
- **旧オプションへのフォールバックは意図的に実装していない**
  （既定値が options に載らないホスト挙動のため、読み替えると
  「既定値を選んだときだけ直らない」不具合になる）。
  **既存ダッシュボードで色を設定していた場合は色が既定色に戻るので、
  編集画面の「色分け」で「カテゴリ名|色」を設定し直すこと。**

#### 追加

- 「色分けの呼び名」（`categoryLabel`）。凡例の見出しとフィルタの
  「すべての〇〇」に反映される（例: `ログ種別`）。空欄なら非表示。
- 色指定で基本的な CSS 色名（`red` / `lime` / `navy` 等 22 色）を使えるようにした。
- 色分け列の値が空の行を `(未分類)` という独立カテゴリとして扱う
  （従来は `Low` に丸めていた＝深刻度前提の挙動だった）。
- **ホイールズーム / ドラッグ移動**（地図アプリと同じ操作感）。
  マウスホイールで**カーソル位置を固定点に**拡大縮小（`1`〜`40` 倍）、ドラッグでパン、
  ダブルクリック（または右下の `×N ⟲`）で初期表示に復帰する。
  `enableZoom` を OFF にすれば従来どおりの固定表示にできる。
- **中心の経度・緯度オプション**（`centerLon` / `centerLat`）。
  任意の地域を中心に据えて表示できる（例: 経度 `135` / 緯度 `35` で日本中心）。
  `initialZoom` と併せて「初期表示の画角」を編集画面から指定できる。
- **ズーム段階に応じた地名ラベル**（`showPlaceLabels`）。
  低ズームでは国名のみ、拡大するにつれて主要都市 → 地方都市が現れる。
  ラベルは重なりを自動判定して間引き、画面外のものは描かない。
  文字サイズは `placeLabelSize`（既定 11px）で調整できる。
- **矢印の始点・終点の地点名を地図上に常時表示**（`showEndpointLabels`）。
  `src_name` / `dst_name` の値を使い、地図由来の地名より優先して場所を確保する
  （データが主役であるため）。従来どおりホバー時の詳細ツールチップも出る。
- 都市名データとして **Natural Earth `ne_50m_populated_places`（約 1,250 都市・パブリックドメイン）** を同梱。
  国名は既存の `world-atlas`（Natural Earth 由来）から取得する。**いずれもクレジット表記不要**。

#### 変更

- 投影方式を `fitExtent` + `preserveAspectRatio="none"`（引き伸ばし）から、
  **中心経緯度 ＋ ズーム倍率で駆動するカメラ方式**へ変更した。
  経度方向は `rotate`（東西の継ぎ目が出ない）、緯度方向は投影後の平行移動で中心を寄せる。
  これに伴い**地図の縦横比の歪みが解消**され、形が正しく描かれるようになった。
- 描画とズーム計算が同一の投影生成関数（`makeProjection`）を通るようにした。
  両者がずれるとホイールズームがカーソル位置から外れるため、単一の情報源にまとめている。

#### 修正

- ホイールイベントでポインタ座標が取得できない環境（合成イベント等）で、
  `NaN` が投影計算に伝播して**ズームが無反応になる**問題を修正（パネル中心を固定点にフォールバック）。

#### 成果物

- `dist/custom_viz_worldmap-1.4.0-ea92401.spl`
- ローカル検証（`yarn verify`）: **77 件すべて成功**
  （ズーム・地名の検証 13 件、色分け一般化の検証 17 件を追加。
  「深刻度と無関係なデータセット（列名も値も severity 由来でない）で
  指定どおりに色が付く」ケースを含む）

---

### [1.3.0] - 2026-07-25

#### 変更

- **Severity の色設定を「1本の順序付きパレット」に統合した**（`editor.seriesColors` の `severityColors`）。
  従来は「High / Medium / Low という固定名に紐づく専用色3つ ＋ それ以外を受け止める無名の予備パレット」という構成で、
  **`high` / `medium` / `low` に完全一致する語彙を使っている場合しか名前付きの色が当たらない**という制約があった。
  `P1`/`P2`/`P3`、`緊急`/`警告`、`tier1`/`tier2` のような独自の語彙を使うデータでは、
  すべての Severity が予備パレット送りになり、意味づけができなかった。
- 新方式では、サーチ結果に登場した Severity を **凡例の並び順**（既知順 `critical`→`high`→`medium`→`low` を先頭に、
  残りは登場順）に並べ、その順に `severityColors` パレットを上から1色ずつ配る。
  **どんな語彙でも全カテゴリに別々の色が付き、ユーザーが各色を自由に指定できる**。
  カテゴリ数がパレットの色数より多い場合は循環する。
- 既定パレット（`#ff5a2e` / `#e6b93c` / `#38a6ff` / `#b17aff` / `#2dd4bf` / `#ff7ab8` / `#9aa7b8`）は、
  よくある `high` / `medium` / `low` の語彙のとき **v1.2.0 とほぼ同じ見た目**になるよう並べてある。
- **`editor.threshold` は採用していない**。この viz の Severity は文字列カテゴリであり、
  `editor.threshold` が扱う数値レンジには原理的に対応しないため。数値レンジではなく順序付きパレットが正しい型。
- 深刻度フィルタ・凡例・弧／ホットスポットの描画は、任意個・任意名の Severity に対して従来どおり動作する。

#### 削除

- 旧オプション `highColor` / `mediumColor` / `lowColor` / `extraColors`。
  **既定値が options に載らないホスト挙動のため、旧キーへのフォールバックは意図的に実装していない**
  （実装すると「既定値を選んだときだけ直らない」不具合になる）。
  **既存ダッシュボードで線の色を変更していた場合は既定パレットに戻るので、編集画面の「深刻度の色」で設定し直すこと。**

#### 成果物

- `dist/custom_viz_worldmap-1.3.0-fcde869.spl`

---

### [1.2.0] - 2026-07-25

#### 変更

- 「その他の深刻度」の色設定を `editor.color` 4個（`extraColor1`〜`extraColor4`）から **`editor.seriesColors` 1項目**（`extraColors`）へ統合。編集画面が「プリセット選択＋色スウォッチ列」になり、色数もユーザーが増減できるようになった。
- パレットが未知の深刻度の種類数より少ない場合は循環して適用する（既定パレットに落ちない）。
- High / Medium / Low は特定の意味を持つ名前付きの色のため、従来どおり個別の `editor.color`（`highColor` / `mediumColor` / `lowColor`）を維持。

#### 削除

- 旧オプション `extraColor1`〜`extraColor4`。**既定値が options に載らないホスト挙動のため、旧キーへのフォールバックは意図的に実装していない**（実装すると「既定値を選んだときだけ直らない」不具合になる）。既存ダッシュボードで「その他の深刻度」の色を変更していた場合は既定パレットに戻るので、編集画面で設定し直すこと。

#### 成果物

- `dist/custom_viz_worldmap-1.2.0-fcde869.spl`

---

### [1.1.2] - 2026-07-25

#### 変更

- **データ未取得時のメッセージを全 viz 共通の文言に統一**した。
  「データがありません。サーチ結果を確認してください。」（従来は英語表記や viz ごとに異なる文言だった）。
  ダッシュボードに複数の viz を並べたときに、サーチ未設定の空パネルが揃った見た目になる。


#### 生成物

- `dist/custom_viz_worldmap-1.1.2-2dc2dde.spl`

---

### [1.1.1] - 2026-07-22

#### 修正

- **光の帯が終点付近で真っ白に飽和する問題を修正**。加算合成
  （`globalCompositeOperation='lighter'`）で円サンプルを重ね塗りしていたため、
  帯の内部および複数の弧が終点に収束する場所で RGB が飽和し白飛びしていた。
  - 加算合成を廃止し、**通常合成＋純粋な Severity 色のみ**で描画。何本重なっても
    色相が保たれ、白くならない。
  - チープな単色ベタ棒に戻らないよう、帯は**両端が滑らかに窄まるテーパー形状の
    ポリゴンを 1 回塗り**で描画（幅・不透明度とも `sin` エンベロープで両端 0 へ）。
    下に**太く淡い同色グロー**を敷いて柔らかい輪郭を維持。
  - ポリゴン 1 回塗りのためサンプル同士のアルファ累積も起きない。
    ベース軌道・地図・ツールチップ（SVG 側）は変更なし。
- 検証（`test/verify.mjs`）：Canvas スタブに `moveTo`/`lineTo`/`closePath` を追加
  （fill 計数による描画/停止検証は従来どおり）。30 pass。

#### パッケージ
- `dist/custom_viz_worldmap-1.1.1-fcb4ffb.spl`

### [1.1.0] - 2026-07-21

#### 変更

- **攻撃の弧の流れる筋を Canvas 化し「発光する光の帯が飛んでいく」表現に刷新**
  （元の単色ベタ塗りの棒はチープ、SVG dash では進行方向に沿った減衰が作れず彗星の
  先端ドットが浮いて見える、という双方の問題を解消）。
  - 流れる筋だけを **Canvas オーバーレイ**に移設。弧（2次ベジェ）を進行方向に沿って
    サンプリングし、**短い光の帯**（パス長の約22%）を弧に沿って走らせる。
  - チープに見せない工夫：帯を **Severity 色のグロー（太）＋同色の芯（細）の2重＋加算合成
    `globalCompositeOperation='lighter'`** で発光させ、帯の前後両端を `sin` エンベロープで
    滑らかにフェード（棒が急に切れない）。**白い芯ドットは置かず色だけで光らせる**。
    彗星の尾・先端ドットは廃止。
  - 弧の SVG 制御点（`arcControl`）を Canvas サンプリングと共有し、ベース軌道と光の帯の
    軌跡を完全一致させた。
  - ベース軌道（発光ハロー＋芯線）と地図・ホットスポットは SVG のまま。ツールチップも SVG
    側に残し、ホバー可能性を維持。
  - **軽量化**：弧が何本でも Canvas 1枚＋rAF で描画（DOM ノード／フィルタが増えない）。
    `devicePixelRatio` は 2 で頭打ち。`animDuration=0` では rAF を回さず何も描かない（CPU 0）。
- 検証（`test/verify.mjs`）を刷新：happy-dom に Canvas 2D コンテキストのスタブを追加し、
  アニメーション中は Canvas への描画（fill）が発生、静的モードでは発生しないことを検証。
  弧本数は SVG ベース軌道で計数。30 pass。

#### パッケージ
- `dist/custom_viz_worldmap-1.1.0-d0095ca.spl`

### [1.0.1] - 2026-07-21

#### 修正

- **まれにパネルが描画されない事象への対策（マウントゲート導入）**。ホスト初期化完了
  （`DashboardExtensionAPI` 注入＋テーマ／データの初期 state 受信）を待ってから React を
  マウントするよう変更。公式フックは購読登録時に現在値を再送しないため、初期 state が
  マウント後に届くと取り逃して `useTheme` 等が undefined のまま永久に非表示となる
  競合があった。
- **テーマ未取得時のフォールバックを追加**。最大5秒待っても初期 state が揃わない場合は
  light テーマで必ず描画を開始する（永久に真っ白のままになる経路を排除）。

#### パッケージ
- `dist/custom_viz_worldmap-1.0.1-d824e00.spl`

### [1.0.0] - 2026-07-20

世界地図上に位置と攻撃フローを描くアタックマップ。

#### 追加
- 新規作成（初回リリース）。
- パッケージ: `dist/custom_viz_worldmap-1.0.0-beb3d05.spl`
