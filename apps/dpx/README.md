# DPX — DPX

**Splunk 上で動く完全独自のダッシュボード基盤**（アプリ ID: `dpx`）。
Dashboard Studio でも classic（Simple XML）でもなく、独立 React ページの上に自前の
ダッシュボードエンジンを載せたものです。「Splunk 上で動作し、Splunk にサーチが投げられる」
ことだけを前提に、**より映えるダッシュボードを自由に簡単に作れる**ことを目指しています。

実装ナレッジ（構成・スキーマ・viz の作り方・踏んだ罠・E2E ツール）は
[.claude/skills/splunk-viz/references/dpx-platform.md](../../.claude/skills/splunk-viz/references/dpx-platform.md)
に集約しています。**着手前にそちらを読むこと。**

## 構成（30秒版）

```
Splunk Web
 └ ホストビュー dpx（画面はこの1枚だけ。template="pages/splunk_ui_app.html" ＝ Splunk 同梱）
    └ pages/dpx.js                        ← 唯一のランタイム（ホーム＋ダッシュボード）
        /app/dpx/dpx            → ホーム（一覧）
        /app/dpx/dpx?id=<app>/<name> → ダッシュボード
 └ 定義ビュー（1 ダッシュボード = 1 ビュー XML。isVisible=False の入れ物）
    └ <definition><![CDATA[ DPX スキーマ v1 の JSON ]]></definition>
```

- 保存形式は Studio と同型（ビュー XML に定義 JSON を埋め込む）。REST で動的に作成できる
- **画面間は SPA 遷移（pushState）＝ページ再読込ゼロ**。ボード切替・ホーム往復に白は出ない
- viz は「props を受け取る素の React コンポーネント」を `vizRegistry.js` に登録するだけ
  （iframe なし・config.json なし・splunkd 再起動なし）
- **カスタム Mako テンプレートは不使用**（10.4.0 非推奨・AppInspect 4.4.0 から審査 fail の対象）。
  検討した代替案と全数調査の記録は dpx-platform.md §1.1 を参照

## 開発ループ

```bash
cd apps/dpx
rm -rf stage && NODE_OPTIONS=--max-old-space-size=8192 yarn build   # 本番ビルド
yarn package                                                        # dist/*.spl
node ../../tools/dashboard-loop/src/install-viz.mjs $(ls -t dist/*.spl | head -1)
node ../../tools/dashboard-loop/src/shot-page.mjs /en-US/app/dpx/<view> --out /tmp/shots
```

---

## サンプルダッシュボード

| ファイル | 用途 |
|---|---|
| [`examples/aegis-soc.json`](examples/aegis-soc.json) | **ショーケース**。DPX でしか組めない構図を一枚にまとめたもの（下記） |
| [`examples/all-viz-check.json`](examples/all-viz-check.json) | 全 viz の描画確認用（30枚を並べただけ） |

### AEGIS / Global Threat Operations（ショーケース）

3タブ・25パネル・21データソース・14種の viz。**Studio では組めない**要素を意図的に集めてある:

- **左サイドバータブ**（`tabPosition: "left"`）
- **背景エフェクト**（`constellation`）がパネルの裏で常時動く
- **枠なしパネル**（`frameless`）で見出しと時計を地の上に直接置く
- **全幅の地図を1枚のキャンバスとして使う**（`frameless` + 12列。枠もタイトルバーも無い）
- 自作 viz（world-map / attack-globe / sunburst / sankey-flow / heat-matrix /
  timeline-swimlane / liquid-tube）とネイティブ viz を混在

⚠ **地図（world-map / attack-globe）の上に他のパネルを重ねない。**
DPX は `style.z` でパネルを重ね置きできる（Studio に無い機能）が、
**地図に対して使うと肝心の地形と着弾点が隠れる**。
重ねたガラスパネルは見栄えはするものの、地図の凡例・フロー一覧・
端の国（日本／オーストラリア）が読めなくなった（実機で確認して取りやめ）。
**重ね置きは「地図以外」で使う。** 地図は単独で全幅を与えるのが正解。

投入:

```bash
node tools/dashboard-loop/src/push.mjs apps/dpx/examples/aegis-soc.json --name dpx_aegis
```

⚠ **`makeresults format=csv` の CSV は本物の改行で書く。**
`\n` の2文字を埋め込むと**1行の壊れた CSV**になり、
**HTTP 200 のまま 0 行**が返る（エラーにならないので気づきにくい）。
⚠ **`eval` で日本語のフィールド名を参照するときは `'達成率'` と単引用符で囲む。**
囲まないとこれも **200 で 0 行**になる（このダッシュボードの作成中に実際に踏んだ）。

---

## ライセンス

**MIT License**（[LICENSE](LICENSE)）。

バンドルに含まれるサードパーティ OSS の著作権表示・許諾条文は
[THIRD-PARTY-NOTICES.txt](src/main/resources/splunk/THIRD-PARTY-NOTICES.txt) に同梱しています
（配布物である `.spl` にも両方が入ります）。

⚠ `@splunk/dashboard-studio-extension` など Splunk 提供のパッケージは OSS ではなく
Splunk General Terms が適用されます（OSS 通知とは別枠で参照情報のみ記載）。

---

## リリースノート

このセクションは [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) に準拠します。
バージョニングは [SemVer](https://semver.org/lang/ja/) です。

---

### [1.6.0] - 2026-08-12

#### 追加

- **手描き画材の配色プリセット4種**（`style.preset`）。いずれも実在の画材に寄せた設計:
  - **水彩（にじみの紙）** `watercolor` … コールドプレス紙の生成り地に、乾いた縁が中心より
    濃くなる「エッジの濃まり」のウォッシュを4隅に置く。系列色は透明水彩の定番顔料
    （セルリアン・ローズマダー・サップグリーン等）。文字はペインズグレー
  - **クレヨン（黒画用紙）** `crayon` … 手描き系で唯一の暗い地。紙の目（1px の粒）と
    パステルのこすった跡を敷き、系列色は混色しない原色寄りの6色
  - **色鉛筆（スケッチブック）** `pencil` … 細かい紙目の白地。アクセントは校正の青鉛筆、
    エラーは赤鉛筆。系列色は紙の白が透ける「粉っぽい」中彩度
  - **インク＋水彩（ペン画）** `inkwash` … アーバンスケッチ。旅帳の生成り地に群青の
    ウォッシュと紙の日焼け。文字はセピアの製図インク
- **パネル質感4種**（`panel.style.variant`。区画の質感としても使用可）:
  - **水彩（にじみの縁）** … 輪郭線なし。顔料が縁に溜まる inset の濃まりとウォッシュ
  - **クレヨン（蝋の縁取り）** … 太く少し透ける蝋の線（4px・角丸）＋ずらした二度塗り
  - **色鉛筆（ハッチング）** … 2方向のクロスハッチ＋「二度引き」した輪郭線
  - **インク＋水彩（ペン画）** … インクの輪郭線＋線からはみ出すウォッシュ＋ペンの溜まり
- **背景エフェクト「水彩のにじみ（乾いた縁）」**（`style.background: "washBlooms"`）。
  乾いた輪だけを残す静的グラデーション3滴。どのプリセットとも組み合わせ可
- `test/themes.test.mjs` … 全プリセット×全質感の整合テスト
  （`background` 一括と `backgroundImage` の併用検出・区画流用の色差し替え検証を含む）

#### 変更

- 質感の濃度は実機スクリーンショットで2段階調整した（「もっと露骨に」の方向）。
  にじみは当初「内側にも薄膜」だったが、生成り地で灰色に濁るため**縁の輪だけ**に変更

配布物: `dist/dpx-1.6.0-b8dd3f5.spl`

---

### [1.5.2] - 2026-08-12

#### 変更

- **テキスト欄も「確定（フォーカスを外す／Enter）したときだけ」定義（JSON）に書くようにした。**
  v1.5.1 でドラッグに適用した整理を、文字入力にも広げたもの。
  従来は**打鍵ごとに定義が書き換わり**、「タイトルを8文字打ち直す」だけで
  Ctrl+Z が8回必要だった（履歴のまとめキーで誤魔化していた）。
  打っている最中の文字列は**一時的な表示状態**として入力欄の内部にだけ持つ。
  - **Escape** で打ちかけを捨てられる（定義は書き換わらない）
  - **クリアボタン（✕）** は押した時点で確定（ボタンは「操作」なので即書いてよい）
  - IME（日本語入力）は変換中に確定しない。**変換確定の Enter で入力欄を抜けない**
    （`keydown` は `compositionend` より先に来ることがあるため `isComposing` も見る）
- まとめキーの対象が**スライダー・数値欄・矢印キー連打・ソースタブの JSON 直接編集**だけになった
  （いずれも「確定点」を決められない入力）。

#### 修正

- **Escape で打ちかけを捨てたのに、定義に書き込まれていた。**
  Escape は `setDraft` で戻すが**非同期**なので、直後に走る `blur` が読む
  DOM の値には**まだ捨てたはずの文字列が入っていた**。実機 E2E で検出。

#### テスト

- 実機 E2E `tools/dashboard-loop/src/dp-textcommit-e2e.mjs`（新規・17 件）。
  打鍵中／確定／Ctrl+Z／Enter／Escape に加え、**CDP で本物の IME 変換**を起こして確認。
- 生成した `.spl`: `dist/dpx-1.5.2-94d0e38.spl`

---

### [1.5.1] - 2026-08-12

#### 変更

- **ドラッグ中は定義（JSON）を書き換えず、離した時に1回だけ書くようにした。**
  ドラッグ中の座標は「一時的な表示状態」であって保存対象ではない、という整理。
  中間状態を定義に書いていたため、**履歴がドラッグの途中経過で埋まり**、
  それを後段の「まとめキー」で無かったことにする必要があった。
  書き込み側を正したことで、**ドラッグに関するまとめキーが不要になり削除**。
  対象はパネルの移動・リサイズと、区画ごとの移動。
- 結果として **「JSON が1回変わる＝1操作」** が成立し、
  履歴の粒度が特別扱い無しで正しくなった。
- 矢印キーの連打は引き続き1手にまとめる（終わりが明確でない操作のため。
  1マスずつ戻したい場面が考えにくく、連打が数十手に化ける実害のほうが大きい）。

#### テスト

- `applyLayoutPreview()` を追加（`engine/groups.js`）＋単体 11 件。
- 実機 E2E `tools/dashboard-loop/src/dp-dragpreview-e2e.mjs`（新規・12 件）で
  **ドラッグ中に絵が動くこと**と**その間は保存ボタンが押せないこと**（＝定義を書いていない証拠）を確認。
- 生成した `.spl`: `dist/dpx-1.5.1-94d0e38.spl`

---

### [1.5.0] - 2026-08-12

#### 追加

- **編集履歴（Ctrl+Z / Ctrl+Shift+Z）をあらゆる操作に効くようにした。**
  パネルの追加・削除・複製・移動・リサイズ、区画の追加・移動・複製、入力の追加・並べ替え、
  データソースの編集、ソースタブでの JSON 直接編集まで、**定義を変える操作はすべて1つの経路
  （`edit()`）を通る**ようにしたので、履歴に載らない操作が原理的に無い。
- **戻しきったら保存ボタンが押せなくなる。** 「未保存かどうか」を**保存済みの定義との内容比較**で
  判定する（`history.js` の `isDirty`）。Ctrl+Z で戻しきった場合はもちろん、
  **手で元の値に打ち直した場合も**「変更なし」と見なす。
- **1操作＝Ctrl+Z 1回にまとめる**（`coalesceKeyFor`）。ドラッグ中の中間状態や
  連続した文字入力を1手に統合するので、ドラッグ1回が1セルずつ巻き戻ることがない。
- 履歴の上限を 50 → **100 手**に拡大。

#### 修正

- **Ctrl+Z が「1手前」ではなく「2手前」に飛ぶことがあった。**
  履歴に積む定義がレンダー時のクロージャの値で、1レンダー内に2回編集すると
  同じ古い定義が2回積まれていた。
- **矢印キーの移動が Ctrl+Z で戻らなかった。**
  `setDef` の更新関数の中で `setHistory` の結果を外の変数へ書き戻していたが、
  React は更新関数を即時に走らせないので**古い値が返っていた**。履歴を正として
  `setDef` を流し込む形に変更。
- undo/redo の直後に「未保存」が必ず true になっていたのを、内容比較に統一。

#### テスト

- `test/history.test.mjs`（新規・40 件）。
- 実機 E2E `tools/dashboard-loop/src/dp-undo-e2e.mjs`（新規・26 件）で
  入力・ドラッグ・複製・削除・矢印移動・区画追加・redo を通しで確認。
- 生成した `.spl`: `dist/dpx-1.5.0-94d0e38.spl`

---

### [1.4.1] - 2026-08-12

#### 修正

- **区画が「区画外のパネル」に食い込む**のを修正（ユーザー指摘・実測で確認）。
  パネルは元のサイズのまま動かないので、**区画が `gap` を超えて外へ広がると
  必ず隣・下・上のパネルと重なる**。
  - **下・左右**: 以前は左右だけ `gap/2`、**下は生の `pad`** という非対称な実装だった
    （「隣り合う区画の罫が繋がる」問題だけを見て左右を直したため、外側パネルを見落とした）。
    → **4辺すべて同じ規則**（`groupInset()` = `gap/2 − 1px`）に統一。実測 4px → 7px
  - **上**: ヘッダ帯（18px）をそのまま上へ出すと `gap`（12px）を超えるため、
    **区画の真上にパネルがあると 6px 食い込んでいた**（実測）。
    → 最上段だけ帯ぶん伸ばし、途中の行では `inset` までに留める。実測 −6px → +7px
  - ⚠ 上へ伸ばせないぶん**見出しを下へ逃がすと、今度はメンバーのタイトルに重なって
    文字が潰れた**（実機で発生）。→ 帯が取れないときは**罫の上（gap の中）に載せる**。
    gap には他に何も無いので重なりようがない
  - ⭐ **【対応】最上段以外でも「区画タイトルの領域」を確保する**（ユーザー指定）。
    最上段の区画はグリッド上部の余白に見出しを置けるが、途中の行では
    上のパネルとの隙間が `gap` しか無く、**どちらへ逃がしても重なっていた**
    （下＝メンバーのタイトルと衝突／上＝上のパネルへ 5px 食い込み）。
    → **区画が始まる行の手前に、見出し用の細い行（18px）を挿し込む**（`reserveHeaderRows()`）。
    その下のパネルは描画上1行ずつ下へずれる。実測で**上と 17px・下と 15px** の余白を確保
    - ⚠ **定義（`panel.y`）は書き換えない。** 描画時の行番号だけをずらす。
      座標を書き換えると、区画を消したときに元の配置へ戻せなくなる
    - ⚠ `gridAutoRows` では全行が同じ高さになり「見出し行だけ低く」ができない。
      **`gridTemplateRows` を明示**する（見出し行がある時だけ）
    - **見出しを持たない区画・最上段の区画は行を取らない**ので、
      既存ボードのレイアウトは変わらない（間延びしない）
  - ⚠ **枠の箱だけ測って「直った」と誤判定した**（反省）。
    箱は 7px 空いていたのに、**見出しは `position:absolute` で箱の外に出る**ので
    実際には重なっていた。**測る対象を間違えると、数値は嘘をつく**。
    スクリーンショットと**見出しそのものの座標**の両方で確かめる
  - **新規作成／移動のどちらでも同じ**。判定は `rect.y === 0`＝
    **メンバーの座標から毎回計算する**ので、配置の経緯に依存しない
- **区画名の字面がパネルのタイトルと揃わない**のを修正（ユーザー指摘）。
  区画の見出しだけ `fontSize: 10` / 字間 0.22em / 大文字を**決め打ち**していたため、
  パネルのタイトル質感（`panelTitleSkin` の10種）を変えても
  **区画名だけが取り残されて「文字が小さい」「質感が違う」**状態になっていた。
  → **`groupTitleStyle()` で同じ関数から導く**（既定はメンバー先頭のパネルの質感）。
  大きさはパネルより 1px 小さいだけ・**最小 10px** を下回らない。
  親であることは**字間と色**で示し、大きさで差を付けない
- **区画で使えない質感2種を選択肢から除外**（`polaroid` / `punchCard`）。
  - ⚠ **「CSS が同じ＝流用できる」ではなかった。** `groupSurface()` は
    `panelSurface()` と**バイト単位で同じ値**を返すのに、実機では破綻していた。
    どちらも「**中身がある箱**」を前提にした造りで、中身を持たない区画では:
    `polaroid` は白縁（`padding` で中身を押し込んで作る）が成立せず
    **印画面がベタ塗りの明るい面**になってパネルを覆う。
    `punchCard` は **濃い地がパネルの背面全体を覆う**
  - JSON 比較のテストでは検出できず、**スクリーンショットで気づいた**。
    指定されていても既定（`rule`）に落とすので、既存の定義は壊れない
- 生成した `.spl`: `dist/dpx-1.4.1-cef3130.spl`

---

### [1.4.0] - 2026-08-12

#### 追加

- **⭐ パネルのグループ（`groups`）— Studio では原理的にできない意匠。**
  複数のパネルを **1つの区画**としてくくり、区画名を与える。
  「箱が並んでいる」画面を「1枚の管制盤」に変えるためのもの。
  - **Studio が原理的にできない理由**: パネルが iframe に隔離されているため、
    ホストは**パネルとパネルの隙間**に 1px も描けない。パネルを束ねる意匠は作れない。
    DPX は全パネルが同じ CSS grid に載っているので、
    **同じグリッドに背面レイヤとして敷く**だけで済む
  - **枠はメンバーの座標から計算する**（外接矩形）。パネルを動かせば枠が追従するので、
    図形を手で合わせる方式（`shape.nocFrame`）と違い**ズレない**
  - **⭐ 質感は「パネルの質感」をそのまま流用**（区画固有の `rule` ＋ パネルの全20種）。
    区画専用の質感を別実装で持つと**質感を足すたびに2か所を直す**ことになるため、
    `groupSurface()` は `panelSurface()` に委譲するだけにしてある。
    インスペクタの選択肢も `themes.js` の `PANEL_VARIANTS`（1つの一覧）を共有する
    - 色を指定したときは**もともと線を持つプロパティだけ**差し替える。
      ⚠ `panelSurface` は「線なし」を **`border: 'none'`（truthy）** で表すので、
      素朴に `if (s.border)` で書き換えると**コーナーフレームに全周の枠が生え、
      枠なしにも枠が付く**（実際にこの実装で発生させ、値を出力して気づいた）
  - メンバーの質感を「枠なし」にすると、区画がひとつながりの面として読める

```jsonc
"groups": [
  { "id": "g1", "label": "認証系", "panels": ["a1", "a2"], "variant": "rule" }
]
```

- **⭐⭐ 区画ごと移動・複製できる（＝グループが機能単位になる）**
  - **区画名をドラッグ → メンバー全員が相対位置を保ったまま移動**。
    区画を選んで**矢印キー**でも同じ（パネルと同じ操作系）
  - **「区画ごと複製」** … メンバーのパネルもまとめて複製し、新しい区画に入れる。
    「同じ構成をもう1系統」が1操作。⚠ サーチ（`search.ref`）は**共有したまま**
    （データソースまで複製すると同じ SPL が増えて管理が破綻する）
  - ⚠ **クランプは「区画全体」で判定する。** パネルごとに丸めると
    **端に当たった1枚だけが止まって区画の形が崩れる**。
    先に「全体で動ける量」を求めてから全員に同じ量を足す（テストで固定）
  - > **経緯**: v1.4.0 の初版では区画は「枠がパネルの座標を追いかけるだけ」で、
    > **まとめて移動が無かった**（ユーザー指摘）。関係が逆で、
    > **区画がパネルを従える**形にして初めて飾りでなくなる
- **⭐ 区画は「パネル・入力」と並ぶ第3の選択対象**（編集 UI のタイプが増えた形）。
  JSON を書かずに編集画面だけで作れる:
  - **ツールバーの区画ボタン**で追加（選択中のパネルがあれば、それを最初のメンバーにする。
    空の区画は枠が出ないので「追加したのに何も起きない」に見えるため）
  - **キャンバスで区画の見出しをクリック**すると、右ペインがその区画の設定に切り替わる
    （区画名 / 枠の質感 / 枠の色 / 外側の余白 / メンバー一覧＋「外す」/ 削除）。
    選択中は枠が選択色になる
  - **パネルを区画に入れるのはパネル側**の「スタイル → 所属する区画」。
    区画側からパネル名の一覧を選ぶ形にすると**どれがどれか分からない**ので、
    キャンバスで対象を見ながら操作させる
  - 選択は**パネル / 入力 / 区画で排他**（1つ選ぶと他は外れる）
  - ⚠ **区画を削除しても中のパネルは消えない**（枠だけ消える）。
    確認ダイアログにもそう明示する

#### 意匠の詰め（2x 拡大のスクリーンショットで直したもの）

**⚠ 意匠の粗は全画面のスクリーンショットでは見えない。**
1600x1000 の全体像で「直った」ように見えた重なりが、拡大すると残っていた。
`tools/dashboard-loop/src/dp-zoom.mjs`（deviceScaleFactor=2 ＋ clip）で
区画の四隅を切り出して確認する。

- **区画のヘッダ帯を新設**（`GROUP_HEADER_H = 18px`）。
  以前は見出しを枠の**外**（`top:-13`）へ逃がしていたため、
  **区画の上辺とパネルの上端が同じ帯に重なっていた**（ユーザー指摘）。
  → 区画が**自分の見出しの居場所を自分で持つ**形にした。外へ出す限り、
  上にあるもの（パネル・ダッシュボードの見出し）と必ず重なる
- **下辺に「返し」を追加**。上辺の罫だけでは**区画の終わりが分からなかった**。
  ⚠ 最初は**垂直線**にしたが、パネルのカギ括弧と近接して
  「見分けが付かない一塊」に見えた。→ **水平の短い罫**（26px）にして、
  上辺の罫と同じ語彙で「同じものの下端」と読ませる
- **隣り合う区画の罫が繋がって1つに見える**のを修正。
  左右いっぱいに広げていたため罫が突き当たっていた。
  → `gap/2` を超えない範囲に留め、区画と区画の間に必ず切れ目を作る
- **ダッシュボード見出しとの間隔を 12px → 22px** に。
  帯のぶんだけ空けても罫が見出しのすぐ下に来て窮屈だった（実機で計測）

#### 設計上の判断（実機のスクリーンショットで直したもの）

- **当初は四隅のカギ括弧にしたが、パネルの `noc` 質感と見分けが付かなかった**。
  腕の長さを変えても「括弧が並ぶ」だけで階層が読めない。
  → **グループはパネルが持たない device を使う**必要があると判断し、**上辺の罫**にした。
  線1本なので静かで、四隅の括弧（パネル）とは一目で違う
- 見出しの地を塗って罫を切り欠く形は、**背景エフェクトの上で「地色の板」が浮いて見えた**
  ため取りやめ、罫の上に直接置く形にした
- 見出しを `subColor × opacity` にしたら**壁面表示で読めなくなった**
  （カギ括弧を 0.42→0.62 に上げたのと同じ話）。
  弱さは**字間と大きさ**で表し、**色は落とさない**方針に変更
- **最上段のグループの罫がダッシュボードのタイトルを横切る**不具合を修正
  （見出し付きグループが最上段にある時だけ、グリッド上部に隙間を作る）

#### 検証

- 単体テスト `test/groups.test.mjs`（48 件）… 外接矩形・存在しない ID の無視・
  壊れた座標・タブの決定・採番・**割り当て（1枚は常に1区画だけ）**・削除・
  **区画ごと移動（端でも相対位置が保たれること）**。
  ⚠ 枠は1マスずれても見た目で気づけないため数値で押さえる
- 単体テスト `test/groupSurface.test.mjs`（25 件）… **区画の質感がパネルと同一実装**
  であること（色未指定なら `panelSurface` と完全一致）と、
  **色を指定しても質感の構造を壊さない**こと（`noc` に全周の枠が生えない・
  `frameless` に枠が付かない・`underline` の線幅 2px が保たれる）を押さえる
- E2E `tools/dashboard-loop/src/dp-groupmove-e2e.mjs`（5 件・実機）…
  区画名をドラッグ → 保存 → REST で**全メンバーの移動量が同じ**ことを確認。
  ⚠ 「枠が動いた」は検証にならない（枠はメンバーの外接矩形なので、
  パネルが動かなければ枠も動かない）。**パネルの座標そのもの**を比較する
- E2E `tools/dashboard-loop/src/dp-group-e2e.mjs`（9 件・実機）…
  ツールバーで追加 → キャンバスで選択 → 右ペインで改名 → パネル側の「所属する区画」→
  保存 → REST で永続化を確認
  - ⚠ **DPX の `TextInput` は `type` 属性を持たない**（`.dpx-input` クラス）。
    `input[type="text"]` では1件も当たらない（E2E を書いていて実機で判明）
- 実機で `midnight` と `paper`（ライト系）の両方を描画確認。
  ライト系で色が破綻しないことは**必ず見る**（決め打ち色の混入が出るのはここ）
- 生成した `.spl`: `dist/dpx-1.4.0-9f85b82.spl`

---

### [1.3.0] - 2026-08-12

#### 追加

- **⭐ 時間ブラシ（クロスパネル）— Studio では原理的にできない目玉機能。**
  折れ線の上を**横にドラッグするだけ**で、選んだ区間が**ダッシュボード全体の
  時間範囲**になる（時間範囲入力のトークンを書き換えるので、その入力を参照する
  **全パネルが同時に追従する**）。「スパイクを見つけた → その時間だけで全部を見直す」
  という監視で最頻の操作から、時間ピッカーの打ち直しを無くすためのもの。
  - **Studio が原理的にできない理由**: パネルが iframe に隔離されており、
    パネル内のドラッグ座標をホストの時間ピッカーへ渡せない
    （トークン書き込みは合成クリック経由の裏技しかなく、ドラッグの連続値は流せない）。
    DPX は全パネルが同一 React ツリーにいるので `TokenProvider` を直接叩ける
  - ドラッグ中は**選択帯・両端の掴み線・範囲ラベル**（`6.5時間 / 21:17:27 → 03:47:27`）を表示
  - 書き込む値は**絶対時刻文字列**（`2026-08-12T03:47:27`）。時間ピッカーの
    「絶対」タブでそのまま読めるので、**ブラシの後に人が手直しできる**
  - **1バケット以上動かしたときだけ**範囲として扱う（ただのクリックで期間が
    絞られる事故を防ぐ）。**X 軸が時刻として読めないパネル**（ホスト名の軸など）と
    **時間範囲入力が無いダッシュボード**ではブラシ自体を出さない
    （ドラッグできるのに何も起きない、という無反応 UI を作らないため）
  - オプション:「ドラッグで期間を絞る」(`timeBrush`) /
    「書き込む時間範囲入力」(`brushToken`。空なら先頭の時間範囲入力)
- **「絞り込みを戻す」ボタン**（入力バー右端）。時間ブラシ・クリック絞り込みを**1手で戻せる**。
  ⚠ 絞る操作は必ず「絞りすぎ」を起こすので、戻り道が無いと時間ピッカーを
  手で打ち直すことになり、ブラシの速さという利点が消える
  （キオスク表示に ✕ を常設するのと同じ原則）。操作されるまでは表示しない

#### 修正

- **ブラシ中に X 軸のラベルが文字選択されてしまう**のを修正（ユーザー報告）。
  横ドラッグはブラウザから見れば「文字の範囲選択」なので、放っておくと
  軸ラベル（`09:44` `09:49` …）が**青く反転してコピー状態**になっていた。
  → ブラシを出すパネルでは `user-select: none` ＋ `pointerdown` の
  `preventDefault()` ＋ 確定時の `removeAllRanges()` の3点で止める
  （CSS だけだと既に始まっていた選択が残る）
- ブラシのドラッグ開始時に**系列のフォーカス（減光）を解除**するようにした。
  ブラシは線の上を通るため、掴んだ瞬間に「その系列に固定」が効いて
  **絞り込んだ後も他系列が減光したまま**になっていた（実機のスクリーンショットで発覚）。
  ドラッグ確定直後の `click`（系列の固定）も1回だけ捨てる

#### 検証

- 単体テスト `test/timeBrush.test.mjs`（38 件）… **右端バケットを落とさないこと**・
  **タイムゾーンを動かさないこと**（`toISOString()` を使うと JST で9時間ずれる）・
  ホスト名の軸を時刻と誤認しないことを数値で押さえる。
  ⚠ ブラシは1バケットずれても画面上は同じに見えるため、目視では検証できない
- E2E `tools/dashboard-loop/src/dp-brush-e2e.mjs`（8 件）… 実機でドラッグし、
  トークン書き換え・**別パネルの追従**・戻す・ただのクリックで絞られないことを確認
  - ⚠ **「明細の行が変わったか」だけを見る判定は当てにならない**。`makeresults` は
    サーチの度に値が変わるので、**期間が絞られていなくてもテキストが変わる**。
    実際にこれで一度見逃し、スクリーンショットで気づいた
    （軸が 24 時間のままだった）。→ **X 軸ラベルの変化を正とする**判定を追加
- 生成した `.spl`: `dist/dpx-1.3.0-cb825f0.spl`

---

### [1.2.1] - 2026-08-12

#### 修正

- **サーチを Studio と同じ方式に合わせた（`exec_mode=blocking` を廃止）。**
  リバースプロキシ（Cloudflare トンネル）越しにパネルが
  **`[object Response]`** になり描画されない不具合の根本原因。
  - `blocking` は**ジョブ完了まで HTTP 接続を握る**ため、同時に投げた本数ぶん
    サーバ側でジョブが並走し、役割の同時実行上限（`srchJobsQuota`）を超えた分が
    **HTTP 503**（`role-based concurrency limit ... has been reached`）で落ちていた
  - **Studio は `exec_mode` を送っていない**（実機で POST body を観測して確認）。
    同じ実機で標準 Studio ダッシュボードは 12 パネルを 19ms 以内に一斉ディスパッチしても
    **503 が出ない**。キューで絞っているのではなく **blocking を使っていない**ため
  - → DPX も非ブロッキングで投げ、**`dispatchState` のポーリングで完了を待つ**方式へ変更
  - 実測（同一実機・同時12本）: `blocking` = 201×10 / **503×2** → 変更後 **201×12**
  - **なぜ IP 直では出なかったか**: HTTP/1.1 は1オリジン同時6接続までで、
    ブラウザが**偶然スロットリングしていた**ため上限に届かなかった。
    トンネルは h3/HTTP/2 で多重化が効き、12本が一斉に到達して上限が露出した。
    **Cloudflare が壊していたのではなく、元からあった不具合が見えるようになっただけ**
- **`[object Response]` という無意味なエラー表示を修正**（`handleError` を対で使う）。
  `@splunk/splunk-utils` の `handleResponse` は**生の `Response` を reject する**ため、
  受け側が `err.message` を読むと `undefined` になり `String(Response)` が表示されていた。
  Splunk 自身の `splunk-utils/search.js` は必ず `handleError` と対で使っており、
  これが**サーバの `messages[].text` を本物の `Error` にしてくれる**。
  - 適用先: サーチ実行（ジョブ作成・状態取得・結果取得）と
    `viewStore` の**保存 / 作成 / 共有設定**（同じ潜在バグがあった）
  - 効果: 上限超過や SPL の誤り（例: `Unknown search command '...'`）が**画面に理由として出る**

生成物: `dist/dpx-1.2.1-a1189d4.spl`

---

### [1.2.0] - 2026-08-12

#### 追加

- **コネクタ線 viz（`dpx.linkLine`）を追加**（`components/engine/linkLineViz.jsx`）。
  Studio 拡張の Link Line 相当を DPX ネイティブとして実装したもの。
  パネル同士を線で結び、**サーチ結果の値で線の色が変わる**（死活・遅延の可視化）。
  - **⭐ 編集モードでキャンバス上を直接ドラッグして線を整形できる**
    （点の移動／セグメント中央の「＋」で折れ点追加／中間点をダブルクリックで削除／
    値ラベルのドラッグ移動）。**これは Studio ではできなかったこと**——Studio 拡張は
    パネルが iframe に隔離され、編集モードのポインタ入力をホストがパネル選択に
    使ってしまうため、Link Line は「表示モードで形を整える」しかなかった
  - 色分けは DPX の `editor.colorRules` を流用＝**範囲／一致／グラデーションの3モード**
  - 質感4種（フラット／ソフトシャドウ／ネオン発光／立体パイプ）・矢印・破線・
    流れアニメ・丸端子・値ラベル（単位／接続名／小数桁）
  - データが無くても線は消さず**ニュートラル色＋「N/A」**で描き続ける
  - 表示モードには編集 UI を出さない（壁掛け・キオスクでの誤操作を避けるため）
- **エンジン: `Viz.config.canvasEdit`** を追加。宣言した viz には編集モードの
  **移動用オーバーレイを敷かない**（敷くと viz にポインタが一切届かない）。
  パネル移動はタイトルバー（タイトル非表示時は上端の細い帯）で行う。
- **エンジン: パネルに `data-panel-id` / `data-viz` 属性**を付与。
  E2E がパネルを一意に狙えるようにするため。
- **検証ツール**: `tools/dashboard-loop/src/dp-push.mjs`（DPX 定義の REST push）と
  `dp-linkline-e2e.mjs`（編集モードのドラッグ→保存→REST 永続化の E2E）。

#### 修正

- **`onOptionsChange` が空実装（`() => {}`）で、viz からのオプション書き戻しが
  黙って捨てられていた。** viz 側は保存された前提で描き続けるため、
  **動くのに保存されない**という分かりにくい壊れ方をしていた。`onPatchPanel` に接続。

生成物: `dist/dpx-1.2.0-a1189d4.spl`

---

### [1.1.0] - 2026-08-12

#### 追加

- **トップバーに「Splunk へ戻る」リンクを追加**（`components/engine/SplunkHomeLink.jsx`）。
  DPX は `body > header` を隠して Splunk のヘッダを消すため、これまで**Splunk 本体へ戻る
  導線が画面上に一つも無く**、ブラウザの戻るに頼るしかなかった。
  - 行き先は Splunk のヘッダロゴと同じ **`app/launcher`**（実機の
    `<a data-test="header-logo" href="/en-US/app/launcher">` を確認して合わせた）。
    ロケール接頭辞は `createURL` に付けさせる
  - **ホーム画面（一覧）とダッシュボード画面の両方**の同じ位置（左端）に置いた
  - Splunk 本体へ出るので SPA 遷移ではなく素の `<a href>`。
    Ctrl/⌘ クリックの別タブもそのまま効く
  - 地・文字色は `t.colorScheme` で分岐（ライト系プリセットで読めなくなるのを回避）。
    アイコンは `stroke="currentColor"` でテーマに追随する
  - ⚠ **Splunk のロゴ SVG は複製していない**（登録商標のため）。汎用のホーム記号＋
    「Splunk」の文字で行き先を示す

生成物: `dist/dpx-1.1.0-a1189d4.spl`

---

### [1.0.0] - 2026-08-12

**初回リリース。** DPX（独自ダッシュボード基盤）の最初の正式版。
これ以前の 0.x は開発版のため、リリースノート・配布アーカイブとも本版に集約している。

#### 主な機能

**基盤**
- Splunk 上で動く**完全独自のダッシュボード基盤**（Dashboard Studio でも classic でもない）。
  独立 React ページの上に自前のレンダリングエンジンを載せた構成
- 画面はホストビュー 1 枚に集約した **SPA**（`?id=<app>/<name>` で切り替え。画面遷移で再読込なし）
- ダッシュボード定義は**ビュー XML 1 枚＝1 ダッシュボード**（classic と同じモデル）。
  REST で動的に作成・更新できる
- **Mako 不使用**（Splunk 同梱の標準テンプレートを使用）。AppInspect は
  cloud / private_* すべて **failure 0**

**作成・編集**
- GUI での**ダッシュボード編集**（パネルのドラッグ移動・リサイズ・複製・重なり順）
- **共有データソース**（1 つのサーチを複数パネルで参照しても実行は 1 回）
- **タブ**（上部／左サイドバー）と自動送り、**入力とトークン**、**パネル間のクリック連携**
- **SPL エディタ**（Splunk 公式の Ace＋SPL モード。構文ハイライト・整形）
- **設定の別ウィンドウ表示**（URL バーの無い浮遊ウィンドウ。マルチモニタ対応。
  ダッシュボードを全幅で見たまま調整できる）

**見た目**
- **配色プリセット 13 種**（画面発光系に加えて、紙・活版／青焼き図面／熱画像／電子ペーパー）
- **パネル質感 19 種**（コーナーフレーム・すりガラス・活版・印画紙・パンチカードなど）
- **背景エフェクト 22 種**（canvas 系・パターン系・グラデーション系）
- タイトルの位置と質感、パネル個別の色・角丸・傾きなどの上書き

**viz**
- **DPX ネイティブ viz**（表・棒・折れ線／エリア・ドーナツ・単一値・ランキング・ステータス一覧）
- **装飾・図形**（テキスト・時計・図形・コーナーフレーム）
- **既存のカスタム viz 31 種がそのまま動く**（iframe なし・Studio と同じ type 指定）
- **値→色のルール**を UI から設定可能（範囲・一致・グラデーション。Studio では DOS の手書きが必要だった領域）

#### 動作環境

- Splunk Enterprise 10.4 系で開発・実機検証
- 一部の viz は WebGL2 を使用（対応環境が必要）

#### ライセンス

- **MIT License** で公開（`LICENSE` を配布物にも同梱）

生成物: `dist/dpx-1.0.0-<hash>.spl`
