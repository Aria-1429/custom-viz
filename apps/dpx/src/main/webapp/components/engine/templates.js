// ── 新規ダッシュボードの初期定義 ──────────────────────────────────
//
// 【2026-08-10】テンプレートギャラリー（空白 / SOC ウォール / KPI ボード / 監視）は
// **廃止した**。デモ SPL 入りの完成形を配ると、
//   - 作成直後に「自分のサーチに差し替える」作業が必ず発生する
//   - makeresults のダミーが消し忘れられて残る
//   - テンプレート側の定義が viz の仕様変更に追随せず腐る
// という問題があり、雛形としての価値より負債の方が大きかった。
//
// 新規作成はつねに空のキャンバスから始め、
// 編集モードのツールバーから欲しいものを足していく形にする。
// ────────────────────────────────────────────────────────────────

/** 新規ダッシュボードの空定義。 */
export function emptyDashboard(title) {
    return {
        version: 1,
        title,
        description: '',
        style: { preset: 'midnight' },
        chrome: 'dpx',
        grid: { columns: 12, rowHeight: 72, gap: 12 },
        inputs: [],
        panels: [],
    };
}
