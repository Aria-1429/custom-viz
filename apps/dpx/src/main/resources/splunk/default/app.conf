[id]
name = dpx
version = 1.2.1

[ui]
is_visible = 1
; ⚠ AppInspect の check_for_valid_ui_label は **5〜80 文字**を要求する。
;   「DPX」の 3 文字では cloud / private_* タグで failure になる（実測）。
label = DPX Dashboards
supported_themes = light,dark

[launcher]
author = custom-viz
description = DPX — Splunk 上で動く完全独自のダッシュボード基盤（GUI 編集・共有データソース・カスタム viz 30 種対応）
version = 1.2.1

[package]
check_for_updates = 0
id = dpx

[install]
is_configured = false
build = 1
