# ビジュアライゼーション新規候補（2026-08-06 ブレスト）

既存29 viz と被らない「とんでもない」新規 viz の候補集。
実機検証済みの知見（WebGL2 が iframe 内で 62fps／透過3点セット／`editor.threshold`／
クリックによるトークン設定／地点クラスタリング／Natural Earth 同梱）を組み合わせて立案した。
**採用判断待ち。実装着手時は各案の「未検証・推測」を Editor Probe / webgl-probe 相当で先に潰すこと。**

> 表記ルール（スキル準拠）：（実機確認済み）＝過去に実機で確認した事実。
> （未検証・推測）＝コード・仕様からの推測。着手前に要検証。

---

## 案1：Attack Globe — レイトレース3D地球儀 ★推奨 → **✅ 実装済み（2026-08-06, v1.1.0）**

> `visualizations/attack-globe/` として実装した。ドリルダウンは world-map と同じ
> SVG ノード登録方式を採用（自前ピッキング不要だった）。昼夜ターミネーターも実装。
> 実機での表示・ドリルダウン確認と examples/example.png の撮影が残タスク。

**コンセプト**：world-map の「とんでもない版」。平面地図ではなく、フラグメントシェーダで
レイトレースした本物の球体地球を回し、表面に攻撃アークを3Dで飛ばす。

- 球はレイ・球交差の解析解で描画（SDF レイマーチ不要で軽い）。
- 大気の縁はフレネルで青く発光（liquid-tube で実証済みの式を流用）。
- 昼夜のターミネーター（明暗境界）は法線と光源の内積で算出。
- 陸地は Natural Earth（PD・クレジット不要）を事前に等距円筒テクスチャへ焼いてバンドル。
  MAX_TEXTURE_SIZE=16384 確認済みなので 8192×4096 で十分精細（実機確認済み）。
- 弧は緯度経度→3D大円補間、地表から浮かせて描く。
- world-map の資産を球面座標へ移植：「カテゴリ|色」指定・threshold バンド・
  近接クラスタリング・慣性ドラッグ。
- ドラッグで回転／ホイールでズーム。放置時はゆっくり自転（周期オプション、0で停止）。

**未検証・推測**：
- WebGL 面クリックからのドリルダウン。クリック座標→逆投影の自前ピッキングが必要。
  canvas の click は通常の DOM イベントなので `events` 宣言＋クリックで通るはず（未検証・推測）。
  ※ SVG 要素でのトークン設定も「可（未検証）」の状態（[[drilldown-tokens-verified]]）。

**推奨理由**：
1. リポジトリ最強の2資産（world-map の地理ナレッジ × liquid-tube の WebGL ナレッジ）の
   合流点で、未知の要素が最も少ない。
2. SOC 展示価値（見た目のインパクト）が最大。
3. 平面 world-map と共存できる（置き換えではなく「回る方」）。

---

## 案2：Weather Panel — システム状態を「天気」で語る全面オーバーレイ

**コンセプト**：spotlight-frame の「枠だけ表示して重ねる」発想と、WebGL の透過3点セット
（実機確認済み）を合体。ダッシュボード全体に透明パネルとして重ね、状態を天気で演出する。

- エラー率が上がると FBM ノイズの雲が厚くなり、雨脚が強まる。
- CRITICAL でパネルの上から稲妻が走る。平常時はほぼ何も描かず透明。
- 「数字を読む前に空気で分かる」壁掛け SOC 向け。
- 判定は spotlight-frame と同じ3モード（自動／数値しきい値／文字列一致）を流用。

**未検証・推測**：
- 常時アニメーションのオーバーレイなので、下のパネルを操作できるよう
  `pointer-events: none` が必要。**iframe 越しに効くかは未検証**。
  （spotlight-frame の frameOnly が既に近いことをやっているので、その実装を先に確認する。）

---

## 案3：Galton Board — イベントが物理で降ってくる分布マシン（最安全）

**コンセプト**：サーチ結果の各行が玉になって釘盤を跳ねながら落ち、下のビンに積もって
ヒストグラムが自然にできあがる。分布を「結果」ではなく「過程」で見せる。

- ビンは値のレンジ。色分けは `editor.threshold`（実機確認済み）。
- 玉の落下数は件数に比例。リアルタイムサーチと組むと流量がそのまま玉の降り方に出る。
- 物理は2D円衝突のみ → canvas 2D で足りる＝WebGL 不要
  （「SVG/canvas で済むものに WebGL を使わない」判断基準どおり）。

**リスク**：技術的な未知がほぼ無い。確実性最優先ならこれ。

---

## 案4：Phosphor Scope — ブラウン管オシロ風時系列

**コンセプト**：時系列を緑蛍光体の残光つきベクタースコープで描く。走査線・ガラス面の
曲面歪み・ブルーム発光をシェーダで再現し、古い管制卓の質感にする。

- 位置づけ：horizon-chart が「密度」を担うのに対し、こちらは「1〜3系列を最高の雰囲気で」。
- 残光は前フレームを減衰させながら重ねる方式。

**未検証・推測**：
- framebuffer の ping-pong（レンダーテクスチャ2枚の交互描画）が必要。
  WebGL2 の標準機能だが、**このリポジトリでは未使用・未検証**。

---

## 案5：Data Nebula — 数万イベントの銀河散布図

**コンセプト**：`stats` の結果ではなく生イベント数万件を GPU パーティクルで星として散布し、
クラスタが星雲のように光る。ズームで個々の星＝イベントに分解。

- x/y はフィールド選択（`editor.columnSelector`。DOS 文字列で届くので要パース＝実機確認済み）。
- 色はカテゴリ列。SVG では数千点で破綻する領域を WebGL の点スプライトで突破する。

**未検証・推測**：
- **ホストが viz に渡す行数の上限**が律速になる可能性（未検証）。
  数万行が `useDataSources` に実際に届くかを最初に確認すること。

---

# 第2弾（2026-08-06 追加）

切り口を変えた追加7案。第1弾が「光学・物理・宇宙」寄りだったのに対し、
こちらは「実在する機械・装置のメタファー」と「有機的なもの」を中心に立案した。

---

## 案6：Split-Flap Board — 空港の発着案内板（パタパタ板）

**コンセプト**：最新のアラート／イベントを、空港・駅の反転フラップ式案内板で表示する。
新しい行が来ると各文字盤がパタパタと回転して文字が入れ替わる。severity-table の
「とんでもない見た目版」。

- 1文字=1フラップ。上下2枚に割れた札が回転する擬似3D は CSS transform（`rotateX`）＋
  React の状態機械で作れる。WebGL 不要。
- 文字が目標に到達するまで中間文字を高速で経由する「回転感」が本物らしさの核。
- 列は `editor.columnMultiSelectionByFieldNameEditor`（実機確認済み）で選択。
- 行の色分け（severity）は `editor.threshold`（数値）＋「文字列|色」形式（link-line 方式）。
- 行クリックでドリルダウン（クリックのトークン設定は実機確認済み）。
- 新着行の検出は前回 rows との差分で行う。リアルタイムサーチと相性が良い。

**未検証・推測**：CSS 3D transform（`perspective`／`backface-visibility`）が iframe 内で
問題なく効くか（通常の DOM 機能なので効くはず。未検証・推測）。

**リスク**：低。技術要素は枯れている。手数（フラップの状態機械）だけ多い。

---

## 案7：Server Rack — 等角サーバーラックのジオラマ

**コンセプト**：ホスト一覧を 19インチラックに実装されたサーバーとして描く。
1行=1Uのサーバーで、LED がトラフィックに応じて明滅し、負荷で前面パネルの色が変わり、
障害機はアンバーの警告灯が回る。データセンターの「現物」をダッシュボードに置く。

- icon-status で確立した多層 SVG＋色ロール方式（実機確認済み）の発展形。
  基準色から上面／前面／側面／LED／陰を導出するので、`editor.threshold` の帯を
  またぐと筐体全体の色が連動して変わる。
- LED の明滅パターンは値（例: EPS、NW流量）から擬似乱数で生成し「通信してる感」を出す。
- ラック数・U 数はデータ行数からオートフィット。ホバーでホスト詳細ツールチップ、
  クリックでドリルダウン（実機確認済み）。
- SVG で完結、WebGL 不要。

**未検証・推測**：特になし（既存技術の組み合わせ）。

---

## 案8：Digital Bonsai — システム健全性を盆栽で語る

**コンセプト**：システムの健全性スコアを1本の木として描く。健全なら枝葉が茂り花が咲き、
劣化すると葉が色褪せて落ち、枯れ枝になる。「今日のシステムの調子」を一目で・情緒で伝える
アンビエント表示。KPI の対極にある「数字を出さない viz」。

- 木は L-system（再帰的な枝分かれ文法）で手続き生成。乱数シードを固定すれば
  同じ木が再現され、健全性スコアで枝の本数・葉の密度・色・垂れ具合を変調する。
- 葉が落ちるアニメーションは canvas 2D のパーティクルで十分。WebGL 不要。
- スコアは gauge-arc と同じ集計（最終行／平均など）で1値に畳む。
  しきい値→見た目の段階は `editor.threshold`（実機確認済み）。
- 季節オプション（桜／紅葉／雪）を `editor.presetSelector` で一括切替。

**未検証・推測**：特になし。リスクは技術ではなく「絵作りのセンス」に集中する。

---

## 案9：Lava Lamp — メタボールで描くアンビエント状態表示

**コンセプト**：カテゴリ別の件数を、ラバランプの中を漂う「かたまり」として描く。
かたまりの体積=件数で、増えると膨らみ、近づくと粘性のある液体のように融合・分離する。
正確に読む図ではなく、ロビーや壁掛けに置く「動く状態表示」。

- メタボールは 2D SDF の smooth-min（liquid-tube で使った定石の応用）で、
  フラグメントシェーダ1枚で描ける。WebGL2 が使えることは実機確認済み。
- ガラス容器の質感（フレネル・ハイライト）も liquid-tube の資産を流用。
- 背景透過（3点セット、実機確認済み）でダッシュボードに溶け込ませられる。
- カテゴリ→色は「カテゴリ名|色」形式（world-map 方式）＋ `editor.seriesColors`。

**未検証・推測**：かたまり数が多い（>20）ときの SDF 評価コスト（1パスで全ボールを
なめるので fps が落ちる可能性。未検証・推測。ユニフォーム配列の上限も要確認）。

---

## 案10：VU Console — アナログ針が並ぶスタジオ調整卓

**コンセプト**：複数メトリクスを、レコーディングスタジオの VU メーター（アナログ針）を
横に並べた卓として描く。針は物理（慣性＋減衰のバネモデル）で振れ、ピークホールドの
赤針が最大値に残る。デジタル数字を1つも出さない計器盤。

- 対象フィールドは `editor.columnMultiSelectionByFieldNameEditor`（実機確認済み）で複数選択。
- 針の駆動は臨界減衰バネ（2次系）で、値の急変時にオーバーシュートして戻る
  「アナログらしさ」を出す。SVG で完結、WebGL 不要。
- 文字盤のレンジ・単位・red zone は `editor.threshold` で帯指定。
- gauge-arc との棲み分け：gauge-arc は「1値を情報豊かに」、こちらは
  「多数の値を計器盤の雰囲気で並べる」。

**未検証・推測**：特になし。

---

## 案11：Metro Map — 依存関係を地下鉄路線図として自動描画

**コンセプト**：サービス依存関係（`src,dst` のエッジリスト）を、45°スナップの
octilinear（8方向）レイアウトで地下鉄路線図風に自動描画する。駅=サービス、
路線=依存の連なり、駅の色=状態。障害駅は「運転見合わせ」表示になる。

- network-graph（力学モデル）との違いは「路線図の文法」：エッジは水平・垂直・45°のみ、
  駅は等間隔、乗換駅は白丸に黒縁。整った図は掲示物としての価値が段違い。
- レイアウトは力学配置→octilinear スナップ→重なり解消の2段階ヒューリスティクスで
  近似する（完全な octilinear 最適化は NP 困難なので割り切る）。
- 状態→色は `editor.threshold` ＋「文字列|色」形式。駅クリックでドリルダウン（実機確認済み）。

**未検証・推測**：レイアウト品質が入力グラフの形に強く依存する（密なグラフでは
路線図の体をなさない可能性）。アルゴリズム難度は本リポジトリ最高クラス。

---

## 案12：Arcade Defense — ブロックした攻撃が撃墜されるレトロゲーム画面

**コンセプト**：ファイアウォール／WAF がブロックした攻撃を、レトロアーケード風の画面で
「上から降ってくる敵機が、防衛ラインの砲台に撃墜される」様子として描く。
撃墜数カウンタ=ブロック件数。SOC のモニタ壁で最も足を止めさせる1枚。

- 敵機の出現レートを実データ（時間帯別ブロック数）に比例させ、
  種類（攻撃カテゴリ）で敵のドット絵と色を変える。
- ドット絵は自作の 8×8 スプライト（ライセンス問題なし）。canvas 2D で完結。
- 「ゲームであってゲームでない」：ユーザーは操作しない。砲台は自動迎撃で、
  取りこぼし（=許可してしまった攻撃、があれば）は地面に着弾して減点表示。
- スキャンライン・CRT 湾曲などの化粧は任意（入れるなら案4と同じシェーダ技術）。

**未検証・推測**：特になし（canvas 2D のみなら）。リスクは「ふざけすぎ」と受け取られる
可能性という組織文化面のみ。真面目モード（演出控えめ）のプリセットを用意して緩和。

---

## 検討順序（着手時）

1. 案1（Attack Globe）：references/studio-extension-viz.md と
   references/webgl-in-custom-viz.md を読む → canvas クリック経由のトークン設定を
   Editor Probe で検証 → テクスチャ焼き込みパイプライン → 本実装。
2. 確実性を優先する場合は案3（Galton Board）から。
3. 案2・4・5 は各「未検証」項目のプローブ結果を見てから判断。

第2弾の目安：
- **未知が少なく確実に作れる**：案7（Server Rack）・案10（VU Console）・案8（Digital Bonsai）
  ＝既存技術の組み合わせのみで、未検証項目が実質ゼロ。
- **見た目インパクト優先**：案6（Split-Flap Board）・案12（Arcade Defense）。
- **技術チャレンジ枠**：案11（Metro Map）はレイアウトアルゴリズムが本リポジトリ最難。
  案9（Lava Lamp）は SDF コストのプローブを先に。
